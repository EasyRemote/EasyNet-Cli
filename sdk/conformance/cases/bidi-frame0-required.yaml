id: bidi/frame0_required
profile: runtime_core
required_for:
  - rust
  - c_abi
  - go
  - python
  - node
  - java
  - swift
description: Bidi session open requires an explicit frame0/opening payload so a session cannot enter the runtime without caller-visible initial causal input.
steps:
  - action: open_bidi_without_frame0
  - action: project_typed_rejection
expect:
  result: rejected
  missing_frame0_rejected: true
