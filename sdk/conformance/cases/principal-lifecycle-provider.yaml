id: principal/lifecycle_provider
profile: principal
required_for:
  - go
  - python
description: Go and Python expose one product-neutral RuntimePrincipalProvider that lowers lifecycle transitions through generic runtime abilities without private-key or product account fields.
steps:
  - action: require_principal_states
  - action: require_key_binding_states
  - action: require_lifecycle_operations
  - action: require_runtime_principal_provider
  - action: require_principal_command_lowering
  - action: reject_private_key_fields
  - action: reject_product_account_fields
expect:
  result: principal_lifecycle_provider
  provider_required: true
