// Code generated by tools/scripts/generate-runtime-governance-routes.py. DO NOT EDIT.

export const RUNTIME_ABILITY_DESCRIPTOR_PROVIDER = "ability_descriptor";
export const RUNTIME_RECEIPT_HISTORY_PROVIDER = "receipt_history";

const RUNTIME_GOVERNANCE_ROUTES = Object.freeze([
  ["meta.list_abilities", RUNTIME_ABILITY_DESCRIPTOR_PROVIDER],
  ["meta.list_resources", RUNTIME_ABILITY_DESCRIPTOR_PROVIDER],
  ["invocation.history.list", RUNTIME_RECEIPT_HISTORY_PROVIDER],
  ["invocation.history.get", RUNTIME_RECEIPT_HISTORY_PROVIDER],
  ["invocation.history.path", RUNTIME_RECEIPT_HISTORY_PROVIDER],
  ["invocation.record.get", RUNTIME_RECEIPT_HISTORY_PROVIDER],
  ["invocation.trace.get", RUNTIME_RECEIPT_HISTORY_PROVIDER],
]);

export function canonicalRuntimeGovernanceAbility(value) {
  const clean = String(value ?? "").trim();
  const match = RUNTIME_GOVERNANCE_ROUTES.find(
    ([ability]) => clean === ability || clean.endsWith(`.${ability}`),
  );
  return match?.[0] ?? "";
}

export function generatedRuntimeGovernanceDescriptorProvider(value) {
  const clean = String(value ?? "").trim();
  const match = RUNTIME_GOVERNANCE_ROUTES.find(
    ([ability]) => clean === ability || clean.endsWith(`.${ability}`),
  );
  return match?.[1] ?? "";
}
