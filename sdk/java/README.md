# Java/JVM Daemon SDK

Java/JVM is a P1 facade for enterprise and Android-adjacent integrations. It
must expose stable DTO classes, `AutoCloseable` object lifetimes, typed errors,
and no generated Axon protobufs in public APIs.

Current status: placeholder. See `../SDK_PARITY.md` before claiming package
stability.
