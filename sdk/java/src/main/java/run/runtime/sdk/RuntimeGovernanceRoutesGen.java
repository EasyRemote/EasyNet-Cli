// Code generated by tools/scripts/generate-runtime-governance-routes.py. DO NOT EDIT.
package run.runtime.sdk;

final class RuntimeGovernanceRoutesGen {
  private record Route(String ability, String provider) {}

  private static final Route[] ROUTES = {
    new Route("meta.list_abilities", RuntimeDescriptorRefRequest.ABILITY_DESCRIPTOR_PROVIDER),
    new Route("meta.list_resources", RuntimeDescriptorRefRequest.ABILITY_DESCRIPTOR_PROVIDER),
    new Route("invocation.history.list", RuntimeDescriptorRefRequest.RECEIPT_HISTORY_PROVIDER),
    new Route("invocation.history.get", RuntimeDescriptorRefRequest.RECEIPT_HISTORY_PROVIDER),
    new Route("invocation.history.path", RuntimeDescriptorRefRequest.RECEIPT_HISTORY_PROVIDER),
    new Route("invocation.record.get", RuntimeDescriptorRefRequest.RECEIPT_HISTORY_PROVIDER),
    new Route("invocation.trace.get", RuntimeDescriptorRefRequest.RECEIPT_HISTORY_PROVIDER)
  };

  private RuntimeGovernanceRoutesGen() {}

  static String canonicalAbility(String value) {
    String clean = value == null ? "" : value.trim();
    for (Route route : ROUTES) {
      if (clean.equals(route.ability()) || clean.endsWith("." + route.ability())) {
        return route.ability();
      }
    }
    return "";
  }

  static String descriptorProvider(String value) {
    String clean = value == null ? "" : value.trim();
    for (Route route : ROUTES) {
      if (clean.equals(route.ability()) || clean.endsWith("." + route.ability())) {
        return route.provider();
      }
    }
    return "";
  }
}
