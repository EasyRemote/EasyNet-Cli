# Swift Daemon SDK

Swift is a P1 facade for macOS/iOS-adjacent clients. It must expose async/await
clients, value DTOs, explicit close/cancel operations, and no Axon symbols in
the public package API.

Current status: placeholder. See `../SDK_PARITY.md` before claiming package
stability.
