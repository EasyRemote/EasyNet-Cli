{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://easynet.run/sdk/schemas/publication.schema.json",
  "title": "PublicationRecord",
  "type": "object",
  "additionalProperties": true,
  "required": ["profile", "kind", "descriptor_ref", "owner_ura"],
  "properties": {
    "profile": {"const": "publication"},
    "kind": {"type": "string", "minLength": 1},
    "descriptor_ref": {"type": "string", "minLength": 1},
    "owner_ura": {"type": "string", "minLength": 1},
    "resource_ref": {"type": ["string", "null"]},
    "status": {"type": ["string", "null"]},
    "metadata": {"type": "object"}
  }
}
