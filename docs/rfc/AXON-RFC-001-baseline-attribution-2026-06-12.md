# RFC-001 baseline 漂移归属表(T0.1 签字前置件,2026-06-12)

> **CURRENT STATUS(2026-07-02)**: P4.8d facade MCP quarantine anchor has now
> been retired in the live tree. The active baseline is generated by
> `engineering/scripts/check-rfc-001-conformance.sh` and recorded in
> `docs/rfc/AXON-RFC-001-baseline-counts.txt` as total 842. The table below is
> retained only as the historical June 12 attribution record.

> **SUPERSEDED(2026-06-12 当日)**:本表为第一轮归属(基线后 +39 的三行分解,HEAD 较旧)。
> 签字请以 **AXON-RFC-001-baseline-delta-attribution-2026-06-12.md**(e9d6d92,快照法对
> 757a917/4e1536b 差分,现值 927)为准——裁决卡 #4 已指向该表。本表留档仅作方法对照。

> spec T0.1:「逐条审 917−888=29 条违规归属…不一次性吞」。本表为逐行差分实测
> (基线文件 vs 当日实跑;实跑已再涨至 927,差分同口径覆盖)。

## 差分:888 → 927(+39),全量分解为三行

| 规则行 | 基线 | 现值 | Δ | 归属与处置 |
|---|---|---|---|---|
| Rule 1 · publish_system_abilities workaround | 1 | 0 | **−1** | **改善**:PR#5 workaround 已被移除,无需处置 |
| Rule 2 · AgentType 内部 enum | 148 | 156 | +8 | **既裁定豁免行**(P4.4 决议「kept; no wire impact」):agent-type 机制在豁免伞下自然生长,非新违规 |
| Rule 3 · MCP keyword in CLI src | 720 | 752 | +32 | **既裁定待删行**(P4.8d facade/mcp 移除待办,Rule 1 类):增长全系 MCP 相关开发,随 P4.8d 批次整体清零 |

**其余全部规则行零漂移。** 无新违规类型;原审计记录的「本会话 +1」(mcp_executor
文案)已被 +32 吞并于同一已裁定行。

## 推荐处置(供 T0.1 签字)

1. **按真值更新 baseline-counts.txt**(888→927),本表作为归属记录随提交引用——
   不构成「一次性吞」:被吞的只有两个已有明确处置决议的桶。
2. **结构性建议(可一并裁)**:baseline-lock 从「总数比较」升级为**逐行比较**——
   新行或非豁免行增长 = fail;豁免行(AgentType/P4.4、MCP/P4.8d)增长 = warn。
   归属从「事后人工考古」变为「锁的结构性质」,本表此后自动化产出。
3. P4.8d 执行时,Rule 3 的 752 应一次清零——届时 baseline 再更新一次即终态。
