# T2.1b 施工准备:backend 退役 `runtime.invoke_remote` 包装(F-040 收口,2026-06-12)

> 状态:**execution-ready,等 T2.1 关账即开工**。观测到 T2.1 step-5 hub 侧在制
> (Axon WIP:client-sdk invoke.proto 同步 + runtime-rs bidi_handler);本件把 backend
> 侧切换做成「开闸即执行」。盘点全部对照磁盘亲核(backend 干净树)。

## 一、消费面盘点(boundary skill 迁移序第 3 步)

| 文件 | 角色 | 切换动作 |
|---|---|---|
| daemon_grpc/invoke_remote.go(546 行) | 帧构造器:RemoteInvokeParams → InvokeRemoteUpRequest JSON → EnvelopeOpen frame0 | **整文件删除**(「Wire shape mirrors the Rust initiator 1:1」整段消亡) |
| daemon_grpc/remote_routing.go | **降级层**:收 canonical `axon.InvokeRequest`,federation.* 走 unary、device-hosted 降级为 InvokeRemote 包装 | 路由矩阵退役——daemon 拥有 callee 本地性解析(boundary 终局),backend 把完整七元组直接交 Invocation 面;文件瘦身为薄转发或并入 client.go |
| daemon_grpc/reconnecting.go(:326-:346) | RemoteInvoker 的弹性转发实现 | 方法删除(随接口);Invoke/InvokeStream 弹性路径已有 |
| axon/invoke_client.go | RemoteInvoker / RemoteStreamInvoker 接口 + RemoteInvokeParams/OriginCaller/RemoteInvokeResult 类型 | 接口与参数类型删除;OriginCaller 原始字节类型保留(presigned envelope 仍是来源,见三) |
| daemon_grpc/bidi.go / openai 两 handler | 仅注释引用(handler 经 routing client 隔离,**不感知切换**) | 注释随批刷新 |
| svc/servicecontext.go | AxonBidi 暴露注释提及 InvokeRemote | 注释刷新 |
| 6 个 _test.go | frame0/帧形状/回环测试 | 重写为 Invocation 面合同测试(验收:contract test 改打 Invocation 面) |

**分层质量结论**:handler 层经 routing client 完全隔离——切换对产品 handler 零感知,
这正是 2026-06-08 边界审计期望的形态。真实切口只有 daemon_grpc 一层。

## 二、切换后的形状(终局,无过渡垫片;**step-3 落地后精确化,2026-06-12**)

```text
backend handler → daemon_grpc.Client.Invoke(unary)
                  ability = federation.forward_invoke(七元组完整的外层 Invocation)
                  args    = SDK ForwardInvokeRequest(内层 envelope 逐字携带:
                            caller 的 presigned envelope + 内层 ability/args/
                            content + origin claim 字段)
                  daemon 拥有本地性:local-presence → DispatchCall v1(step-2d 写、
                  step-3 设备原生消费,零 JSON 再投影);跨域 → peer hub 升级
```

**判定依据(亲核 HEAD)**:unary 入口的 catch-all 臂(daemon_invocation_service.rs
:806 → dispatch_local_rpc_selected_route :209)只执行本地 LocalRuntime 路由——
Invocation 面的远端设备腿**唯一**经 `federation.forward_invoke` 臂(:775)。
forwarding ability 系 boundary skill 明文许可形;`runtime.invoke_remote` bidi 包装
即此面的非 canonical 旧形,T2.1b 退役之,hub 侧 invoke_remote 受理臂随 step-5 删。

- backend 不再区分「device-hosted vs federation.*」——这是 daemon 的 callee
  本地性解析职责(boundary skill 终局裁决)。
- OriginCallerClaim 不再需要 backend 构造:presigned envelope 在 InvokeRequest 上,
  daemon 自取(fe6060e7 构造器届时降级为 daemon/Cli 内部使用)。
- **SDK 连带退役**(后续批,T2.1 全关后):Axon sdk/go invoke_remote.go 的
  InvokeRemoteUpRequest JSON 家族随两端切完删除——SDK 不为死 wire 保留形状。

## 三、不动面(防过切)

- `axon.OriginCaller` 原始字节类型:presigned envelope 的进程内载体,与 wire 无关,留。
- LivenessProbe / IsAxonLive:传输活性,与载体无关,留(盘点 §三 评审已裁产品层可留)。
- reconnecting.go 的 Invoke/InvokeStream 弹性逻辑:不动。

## 四、搭车与勘正

- **F-044 勘正**:陈旧 cliipc 注释实为 **3 处**(spec 记 2)——client.go:12、
  servicecontext.go:179(自 :172 漂移)、**mapping.go:348(新发现)**。随批清。
- 验收(spec T2.1b 行):invoke_remote.go 删除;contract test 改打 Invocation 面;
  grep cliipc 零命中(3 处全清后)。

## 五、序列(开闸后单会话一批可完)

1. remote_routing 路由矩阵 → 直通 Invocation 面(handler 零改动,测试先行);
2. invoke_remote.go + RemoteInvoker 接口族 + reconnecting 转发删除;
3. 6 测试文件重写为 Invocation 面合同测试;
4. F-044 三处注释 + bidi.go/servicecontext.go 注释刷新;
5. go build ./... + go test ./...(33 包基线)+ e2e invoke 流。

## 六、第 47 轮修订:三道 daemon 侧运载缺口,切换暂缓(2026-06-12)

step-0(Go SDK forward 形,Axon ec0b7a60)落地后的施工核验**推翻「单批可完」**:
`federation.forward_invoke` 今日形状是 CLI 桥的素朴 unary 形,backend 真实面有三样
它运不动(全部 file:line 亲核):

1. **流式**:remote-stream 腿是活生产(invokeAbilityStreamHandler.go:52、openai
   chat_completions.go:287 经路由客户端流式打设备 agent);daemon InvokeStream 面
   `_other` 臂仅本地路由(daemon_invocation_service.rs:852),无远端 forward 臂。
2. **内容信封**:ForwardInvokeRequest 无 content 字段;invoke_remote 帧携
   args_content_envelope(加密会话依赖)。
3. **权威字段**:SessionAuthority 活跃于 6+ handler 站点(file/terminal/remotedesktop/
   pages/openai),Delegation 同道;forward 形无运载位。

**裁决**:不做按特性分叉的半切(比单一 invoke_remote 路径更糟)。完整 T2.1b 前置 =
daemon 侧补运载,**设计问题路由载体会话**:(a) 扩 forward_invoke 形(+content/
authority/流式变体),或 (b) 按 commit-plan 字面「backend direct InvokeRequest」——
给 unary/stream catch-all 长远端臂(canonical InvokeRequest 直接受理,callee 本地性
解析含远端,content/authority 本就在 InvokeRequest 上,流式天然)。**倾向 (b)**:
不养肥转发包装,且与 step-3 设备侧「七元组直入」对称。
step-0 的 SDK 形不浪费:(a) 案直接用;(b) 案下 forward_invoke 仍是跨域腿的形。

## 七、第 50 轮终形:(b) 案裁定后的切换形(2026-06-12)

第 49 轮回应裁定 (b)(且指出非新裁——spec §0.2:48 终局字面即此;决策密度家规
反向命中本会话:路由前未查已批文)。(b) 案下本件 §一/§五 的切换序列**再简化**:

```text
backend handler → daemon_grpc.Client.Invoke / InvokeStream(canonical
                  InvokeRequest 原样直交;content/authority/流式字段本就在
                  请求上,零包装零改写)
daemon          → unary/stream catch-all 长远端臂(载体会话施工中):
                  callee 本地性解析 → 本地 LocalRuntime / local-presence
                  DispatchCall v1 / 跨域 forward(ec0b7a60 形仍服务此腿)
```

- **backend 路由 adapter 整体退役**(已落地):daemonInternalAbility 分类器、
  remoteTransportCaller、originCallerFromReq 全部消亡——分类与 origin 提取都归 daemon。
- invoke_remote.go + RemoteInvoker/RemoteStreamInvoker 仅保留底层 bidi helper;
  默认产品调用路径不再使用它们包装普通 Invoke。
- 等待信号已关闭:daemon unary catch-all 远端臂已承接本地性解析与 session.open 转发。

## 八、第 55 轮:窗口开启,daemon 远端臂施工图(行锚亲核)

幻影占用拆穿(e1abba0:53/57 系 rustfmt 残渣),transport 归零,step-4 解锁。
unary 远端臂的精确施工图:

1. **解禁点**:unary_dispatcher.rs resolve_local_rpc_route :198-205 的
   `route_selected_remote_host_status` 硬拒——self 判定移出 resolver,落到
   dispatch_local_rpc_selected_route 的分支点(self → 既有本地路;remote → 新臂)。
2. **共享派发核**(DEC-F004 单结算路纪律):从 dispatch_local_presence_forward_invoke
   :1370-1522 提取 `dispatch_frame_to_presence` 核——pending 守卫 / lookup_tracked /
   register_pending_for / try_send 双离线映射 / await_reply / DispatchResult 解包。
   forward 臂与新远端臂同核,不复制。
3. **新臂 dispatch_remote_rpc_selected_route**:envelope 必填(canonical 面,缺 =
   invalid_argument);载体选择同 :1405-1435 模式——v1 槽 → build_carrier_v1_dispatch_frame
   (envelope_with_selected_callee 钉 resolver 裁定 callee,arguments 原样);v0 槽 →
   build_invoke_remote_dispatch_frame + OriginCallerClaim(由 envelope presigned 三元 +
   nonce + 公共名构造——backend originCallerFromReq 的 daemon 侧等价物)。
4. **receipt**:新臂接 step-2c 的 ledger_record_from_remote_receipt(forward 臂的
   `receipt: _` 注释「rides T2.1b」即此);首拍可随臂落或紧随小提交。
5. **测试**(daemon_invocation_service_tests 模式,oneshot 裁决 + 超时,8ee1c49 模板):
   远端 v1 happy path / v0 JSON 回退含 claim / 目标离线 fast-fail / 无 envelope 拒绝。
6. stream 臂与 backend 整 fork 退役随后两拍。
