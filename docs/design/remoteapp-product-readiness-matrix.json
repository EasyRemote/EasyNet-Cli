{
  "schema_version": 1,
  "product": "RemoteApp interactive desktop",
  "status": "incomplete",
  "product_complete": false,
  "updated_at": "2026-08-29",
  "current_checkout_evidence_audit": {
    "result": "partial_live_runner_evidence_without_signed_campaign_closure",
    "observed": [
      "all current target/e2e/remoteapp-product-completion reports are mode=self-test with evidence_origin=contract_self_test and product_complete_claim=false",
      "all current target/e2e/frontend-remoteapp-browser-lifecycle reports are contract_self_test",
      "all current target/e2e/remoteapp-media-adaptation reports are contract_self_test",
      "nested live_runner strings inside self-test product-completion fixtures are synthetic verifier inputs, not top-level live provenance",
      "the production completion gate rejects contract_fixture payloads even when their evidence_origin string says live_runner, while contract-fixture validation mode can never emit product_complete_claim=true",
      "production completion now requires one DSSE/Ed25519-signed same-run campaign rooted in an external role trust bundle, with clean commit and build digests, selected Resource/session/descriptor/Invocation bindings, Axon-verified receipt hashes, recomputed report/artifact digests, and an atomic replay ledger",
      "no signed live campaign bundle is present in the current checkout, so evidence_origin strings cannot produce a product-complete claim",
      "the 2026-08-29 Linux Browser-to-provider campaign passed direct application, TURN window, TURN application, EasyNet-relay window, and EasyNet-relay application children with live_runner provenance, connected ICE, selected route-class proof, rendered frames, and terminal cleanup; the corresponding reports are retained under target/e2e/remoteapp-{direct,turn-relay,easynet-relay}",
      "the 2026-08-29 EasyNet-relay refresh/resume child passed with a rotated relay lease, same public session, replacement transport, resumed rendered media, and terminal cleanup at target/e2e/remoteapp-easynet-relay/live-linux-display-refresh-resume-20260829-r5/report.json",
      "the 2026-08-29 STUN campaign observed real RFC 5389 bindings in both Colima-to-Docker and reverse VM directions, but both Browser sessions timed out before a connected selected srflx/prflx pair because the VM return route was not viable; failed reports are retained under target/e2e/remoteapp-stun-srflx and no STUN coverage is claimed",
      "the 2026-08-29 Linux application EasyNet-relay evidence bound an exact two-window process-scoped application set, preserved display_fallback_used=false and scope_widened=false, rendered seven Browser frames, and correctly exposed input_scope=view_only with target_scoped_keyboard_pointer_dispatch_unsafe",
      "the 2026-08-29 macOS signed-helper Browser rerun stopped at the exact Screen & System Audio Recording permission gate while Accessibility was granted; target/e2e/remoteapp-macos-browser/v5-release-signed-helper-live/window/evidence.json is a failed permission artifact, not capture or input success",
      "no Windows host or VM was available for a real capture/input run, so source, cross-build, and mutation evidence remain non-live Windows evidence",
      "the final 2026-08-29 aggregate at target/e2e/remoteapp-product-completion/final-20260829/report.json failed with 20 explicit errors, product_complete_eligible=false, finalization_state=not_eligible, and product_complete_claim=false because the signed campaign plus 19 required production reports were absent",
      "the 2026-08-26 live Browser artifact in /tmp passed the complete authenticated window lifecycle through production create_session/set_description/watch_events/report_client_state/end_session, connected WebRTC, audio and video tracks, four rendered frames, explicit Accessibility policy block, and a caller_ended terminal receipt; it is a local unsigned child proof rather than product-completion authority",
      "the 2026-08-26 live Browser restart-resume artifact in /tmp passed one real macOS window session across an orderly daemon stop/start with the same public session, retired old PeerConnection, newer Runtime transport epoch, replacement connected PeerConnection, watch_events reattachment, one 1688x1080 frame after resume, preserved policy-blocked input authority, and a caller_ended terminal receipt",
      "the 2026-08-26 live Browser application artifact in /tmp passed one real macOS Application Resource lifecycle with a JSON-safe authoritative window-set snapshot, Runtime AppSurface target binding, scope_widened=false, display_fallback_used=false, connected direct WebRTC, one 920x1080 rendered frame, an observed focus_target permission result, and caller_ended terminal receipt",
      "the 2026-08-27 live Linux/X11 Window artifact in /tmp passed one Browser-to-Hub-to-daemon-to-native-provider EasyNet-Relay session across a deterministic resize: the same Window Resource/session/native window and transport epoch were preserved; binding, media-source, and geometry generations advanced; native and decoded frames changed from 480x320 to 640x400; show_session exposed ordered TARGET_MOVED/TARGET_RESIZED; pointer/key down/up were daemon-applied against the new geometry; exact WindowSurface scope and a caller_ended terminal receipt were independently verified. This is an unsigned dirty-campaign child proof, not product-completion authority",
      "the 2026-08-26 reusable host-remoteapp-daemon-sigkill-e2e.sh artifact in /tmp passed one active macOS window session across an exact-PID ungraceful process kill: path-verified daemon PID 4365 was replaced by path-verified PID 53848 at J800, presence moved J700-to-C440-to-J700, the same public session recovered with replacement WebRTC/watch and a newer transport epoch, a post-recovery frame rendered, input authority was preserved, and caller_ended produced a terminal receipt",
      "the same live SIGKILL artifact proved Unix stale-socket recovery: dead control inode 282498224 and Invocation inode 282498231 remained present but unreachable after the crash, daemon listener bind replaced them without manual cleanup with reachable inodes 282510760 and 282510765, and public runtime status reported both endpoints accepting only after J800",
      "the 2026-08-26 host-remoteapp-terminal-receipt-crash-e2e.sh live artifact killed feature-built daemon PID 91393 after authoritative terminal snapshot promotion but before the Browser received end_session, restarted PID 92151 to J800, recovered the same session rdp-0a2fa34448b53eb7dfc35ed4 through public show_session, and proved exact terminal-receipt equality across the private crash marker, authoritative recovery snapshot, paired-user signed public show_session, and repeated public end_session with already_ended=true",
      "the 2026-08-26 reproducible coturn runner artifact in /tmp passed one constrained relay-only Browser scenario with three server-observed allocations, connected/succeeded/nominated ICE, bidirectional bytes, three post-selection presented frames, and terminal cleanup; at that point STUN srflx and EasyNet relay scenarios were still unproven",
      "the 2026-08-26 reproducible direct runner artifact in /tmp passed one Browser macOS/window session with every daemon STUN/TURN/EasyNet relay variable removed, zero projected ICE URLs, host-only local and remote SDP, a connected/succeeded/nominated UDP direct pair, positive bidirectional bytes, three post-selection presented frames, terminal cleanup, and ordinary Runtime restoration",
      "the focused STUN-only runner now uses a provider-host RFC 5389 observer plus an externally reachable VM-NAT Browser topology and directional admission: Browser outbound trickle and embedded offer SDP admit only srflx/prflx, provider inbound retains host/srflx/prflx; projector/verifier mutations reject direct-candidate leakage, wrong-side reflexive selection, missing provider return policy, and absent server binding; the native observer interoperated with coturn turnutils_stunclient and returned a real VM-NAT mapping, but no live Browser selected-pair/media/terminal child exists, so no STUN srflx route is claimed",
      "the 2026-08-26 Docker two-device run in /tmp passed 58 routing and 45 synthetic stream/bidi assertions with distinct caller/provider Device URAs, but used an explicitly recorded 2026-08-25 prebuilt runtime image and dirty source, so it is a lower-bound live regression rather than clean-build product-completion evidence",
      "the 2026-08-23 live artifact paths recorded below are not present in the current checkout and therefore are historical records, not reproducible current evidence"
    ]
  },
  "architecture_invariants": [
    "User Principal is the caller/accountability root, not an Agent.",
    "Device is the execution substrate and key custodian, not the public callee.",
    "SystemAgent owns device-native remote_desktop.* AbilityDescriptors.",
    "A device plugin Ability callee is its catalog owner Agent; the Device is only the execution host, while Service is reserved for stable user-owned product surfaces such as Pages.",
    "RemoteDesktopPlugin is the AbilityImpl.",
    "Display/window/application Resource URA is the Invocation subject.",
    "Every public JSON integer epoch is positive and at most 2^53-1 so Browser clients round-trip target authority without precision loss.",
    "WebRTC/native media is a session transport, not a second Invocation model."
  ],
  "requirements": [
    {
      "id": "application_window_capture",
      "requirement": "Application/window selection and stable capture across macOS/Windows/Linux",
      "status": "partial",
      "current_evidence": [
        "macOS ScreenCaptureKit target model",
        "macOS inventory publishes one application Resource across displays with exact committed window IDs, display IDs, and union geometry",
        "macOS ScreenCaptureKit resolves only owner-matched committed windows into desktop-independent streams and a bounded black-gap multi-surface compositor",
        "application bindings commit ordered front-to-back surface geometry separately from the stable window-set identity, and geometry or z-order drift replaces the complete media plan",
        "macOS application rebind replaces the complete native capture plan across displays",
        "capture-plan rebind prestarts a muted output generation and switches generations only around a successful Runtime-owned binding commit; stale commits resume the old generation",
        "host decoded-frame harnesses",
        "Windows/Linux xcap baseline resolves exact window identity and process-scoped application window sets, observes drift, and fails closed rather than widening to display capture",
        "frontend session details surface daemon target loss reason and recovery action",
        "frontend action row executes daemon-guided refresh_targets recovery by refetching target inventory",
        "check-remoteapp-main-crate-implementation-tests.sh runs main-crate app/window target observation and fail-closed implementation tests",
        "device capability projection advertises production_target_subjects only when the production gate is ready and keeps diagnostic_target_subjects display-only",
        "device capability projection exposes platform_support for macOS/Linux/Windows and labels Windows/Linux exact-target capture baseline_ready rather than production_ready",
        "historical 2026-08-23 record: local product-flow both report passed decoded-frame WebRTC for macOS window and application targets with selected Resource URA subjects and no display fallback; its artifact is not present in the current checkout",
        "the 2026-08-26 live Browser application artifact selected a real System Settings Application Resource whose authoritative inventory was display_scoped=false with resolved_window_ids=[18784], then the Runtime execution snapshot re-bound the exact AppSurface window set to [18784,24147] while preserving requested/effective target kind application, scope_widened=false, and display_fallback_used=false; Browser rendered one 920x1080 frame and closed with caller_ended",
        "the 2026-08-27 live Linux/X11 Browser Window artifact in /tmp preserved one Resource/session/native window and WebRTC transport while a deterministic resize advanced binding/media-source/geometry generations, refreshed the native capture proof from 480x320 to 640x400, presented the replacement decoded frame, emitted ordered show_session TARGET_MOVED/TARGET_RESIZED records, preserved WindowSurface scope, applied rebound pointer/key input, and closed with one terminal receipt; the independent verifier and eight adversarial evidence mutations passed",
        "the current 2026-08-29 Linux Browser artifacts pass exact window and two-window process-scoped application capture over both TURN and EasyNet relay; the application execution snapshot binds both XIDs and their front-to-back surfaces, records display_fallback_used=false and scope_widened=false, and renders sustained Browser frames",
        "the 2026-08-29 macOS signed-helper rerun is permission-blocked before capture because Screen & System Audio Recording is not granted to the exact current helper/daemon identity; Accessibility alone is insufficient and no macOS capture success is claimed from that run",
        "no real Windows interactive host was available on 2026-08-29, so Windows capture remains unverified",
        "remoteapp-cross-platform-capture-e2e.sh verifier for real macOS/Windows/Linux capture or explicit unsupported artifacts with decoded-frame probe evidence bound to target identity/frame source/geometry revision, selected sentinel hash rendering, and no unrelated sentinel leakage for window/application captures"
      ],
      "required_evidence_before_product_complete": [
        "live remoteapp-cross-platform-capture-e2e.sh artifact",
        "real macOS host E2E for display/window/application capture",
        "real Windows host E2E or explicit product unsupported state",
        "real Linux host E2E or explicit product unsupported state",
        "no display fallback for window/application targets",
        "rendered frames for every passing capture scenario",
        "decoded-frame probe bound to selected Resource URA, session id, target kind, frame source id, and geometry revision",
        "selected sentinel content rendered for every passing capture scenario",
        "selected sentinel id/hash observed in decoded frames",
        "unrelated sentinel content absent for every passing window/application capture scenario",
        "visible terminal receipt for every passing capture scenario"
      ],
      "non_claims": [
        "source contracts do not prove real cross-platform capture",
        "the cross-platform capture verifier self-test does not prove live host capture"
      ]
    },
    {
      "id": "input_injection",
      "requirement": "Mouse/keyboard input injection is controllable, low-latency, and permission-correct",
      "status": "partial",
      "current_evidence": [
        "app/window sessions without explicit input-control consent downgrade to view_only with input_consent_required",
        "pointer/key frames are policy-gated",
        "clipboard/file-drop are unsupported",
        "session view projects requested/effective input readiness and blocked reason",
    "frontend input sender consumes daemon input_readiness and fails closed before sending pointer/key frames",
    "frontend input sender refuses to enqueue RemoteApp input when RTC data-channel bufferedAmount exceeds the explicit input backpressure bound",
    "frontend pointer motion is latest-value coalesced to one frame per browser presentation interval so reliable ordered control traffic does not accumulate obsolete coordinates",
    "frontend wheel input is mapped through the exact object-contain target rectangle, normalized from DOM delta modes into bounded CSS pixels, and retried only after separately governed target focus succeeds",
    "daemon wheel input preserves the canonical CSS-pixel contract and performs one bounded platform translation for macOS pixel scrolling, Windows native wheel units, and Linux X11 detents",
    "browser KeyboardEvent.code is parsed once into a plugin-owned physical-key vocabulary shared by macOS, Windows, and Linux adapters",
    "macOS and Windows preserve left/right modifier identity and all three native adapters cover navigation, F1-F12, and numeric-keypad keys instead of silently collapsing common extended keyboard input",
    "frontend input sender attaches monotonic client_sequence and sent_at_ms telemetry to accepted data-channel frames",
        "daemon pointer/key input frame schema accepts frontend sent_at_ms and client_sequence metadata and preserves client_sent_at_ms/client_sequence in input applied/rejected events",
        "daemon input data-channel loop rejects replayed or out-of-order frames as stale_client_sequence when client_sequence is present",
        "daemon applied input events include host_received_at_ms, host_applied_at_ms, and latency_ms when client_sent_at_ms is present and not ahead of host time",
        "daemon applied input events include an opaque stable input_event_id bound to the RemoteApp session id, transport epoch, input kind/action, accepted input count, client_sequence when present, and host receive time",
        "daemon emits INPUT_FRAME_APPLIED for the first successful pointer frame and the first successful keyboard frame while keeping same-kind input storms sampled by a bounded interval",
        "daemon projects runtime OS input permission denial as INPUT_PERMISSION_BLOCKED and deactivates input without failing active media",
        "daemon clears session-local runtime OS input permission blockers after a successful host-applied input frame and projects INPUT_PERMISSION_RESTORED",
        "daemon show_session preserves the session-local runtime input permission blocker in input_readiness.blocked_reason until input is proven active again",
        "diagnostic InvokeBidi input responses preserve client_sent_at_ms and client_sequence for probe correlation, including target_input_not_ready",
        "host view-only input safety E2E opens public remote_desktop.attach InvokeBidi and requires app/window pointer/key diagnostic frames with client_sequence/client_sent_at_ms to reject as input_scope_unsupported",
        "historical 2026-08-23 record: local product-flow both report passed view-only input safety for macOS window and application targets over public remote_desktop.attach InvokeBidi; its artifact is not present in the current checkout",
        "frontend watch_events maps daemon input activation blocks and input frame rejections into visible RemoteApp status without closing media transport",
        "frontend session details separately surface input scope and pointer/keyboard enablement from daemon input_readiness",
        "display interactive sessions require an explicit input-control consent ticket before resolving display_global input scope",
        "window/application sessions with explicit input-control consent resolve target_local scope only when the platform provides press-to-release target isolation; otherwise they remain view_only",
        "target-local admission uses one typed isolation decision: macOS Accessibility/CoreGraphics and Windows xcap/User32 remain implementation candidates, while Linux X11/XTest is explicitly unisolated and Window/Application sessions remain view_only",
        "device input_control_support separately projects target_local_guard_compiled, target_local_runtime_available, and the target-local blocked reason so an available global injector cannot advertise window/application control without an exact-target observer",
        "periodic target observation and per-event input guards use independent bounded lanes backed by the plugin-private easynet-remoteapp-native-host process; invalid, stale, oversized, unsolicited, disconnected, or timed-out generations fail closed and are killed and reaped before replacement",
        "native target observation uses a plugin-private protocol crate and native-host library whose Cargo dependency graph cannot reach the root easynet Runtime crate, Axon, or tonic; the daemon owns only scheduling, process lifetime, wire validation, and domain conversion",
        "Windows input has a User32 SendInput baseline with virtual-desktop coordinate mapping, bounded wheel deltas, keyboard mapping, and partial-application rejection",
        "Linux X11 input has a typed XCB/XTest display-global baseline, but Window/Application input is fail-closed view_only because XTest cannot bind an entire press/release lifecycle to one target",
        "Linux Wayland input fails closed until xdg-desktop-portal RemoteDesktop session identity is bound to the selected Resource",
        "every admitted target_local pointer/key execution re-enumerates host windows and fails closed on identity, display, visibility, focus, geometry, or application window-set/layout drift before platform posting; Linux X11 is not admitted to this scope",
        "target_local pointer execution rejects application-union black gaps and foreign-window occlusion before OS injection",
        "macOS focus observation marks only the first regular visible window of the frontmost process as focused instead of treating every same-process window as the receiver",
        "CLI create-remote-desktop-session exposes explicit --input-control consent while keeping the default no-input grant fail-closed",
        "pointer input frames carry target_geometry_revision when target-local geometry exists and the daemon rejects stale revisions before OS injection",
        "pointer/key input frames carry target_focus_epoch when the daemon projects one and the daemon rejects missing or stale focus epochs before OS injection",
        "frontend input gate rejects missing or stale pointer target_geometry_revision before WebRTC data-channel send",
        "session input_readiness reports target_input_not_ready when target tracking disables input",
        "remote_desktop.permission_status and request_permission now project the actual platform input backend and permission identity: macOS Accessibility, Windows User32 SendInput, or Linux X11/XTest",
        "screen-capture readiness fails closed on Linux Wayland until the portal backend exists and when the daemon has no X11 DISPLAY; request_permission reports requested=true only for an OS permission that can actually be prompted",
        "remote_desktop.request_permission contract and frontend status expose platform-specific input-injection permission alongside Screen Recording",
        "frontend exposes Request permission recovery when daemon input_readiness reports input_injection_unavailable",
        "check-remoteapp-main-crate-implementation-tests.sh runs the main-crate current-session input policy implementation test",
        "device capability projection exposes input_control_support and separates compiled backend state, current-host runtime availability/block reason, and live_e2e_required certification for macOS/Linux/Windows input",
        "remoteapp-input-injection-e2e.sh verifier derives display_global versus target_local from target kind and requires a fresh per-event target guard proof for window/application artifacts in addition to independent OS-effect evidence",
        "host-remoteapp-target-input-e2e.sh drives the production WebRTC media and easynet.remote_desktop.input.v1 data-channel paths in one session, requests explicit input-control consent, observes daemon applied/rejected events, and closes through public watch_events/end_session abilities",
        "the host fixture runs selected and unrelated AppKit processes with independent mouse/key callback logs",
        "the 2026-08-26 real Browser window lifecycle reached connected WebRTC and rendered media, then failed input closed with target_focus_permission_missing because macOS Accessibility was unavailable; it completed end_session and exposed a caller_ended terminal receipt without claiming an OS input effect",
        "resource-derived application target epochs are now canonical positive JSON-safe integers instead of arbitrary u64 hashes, preventing Browser precision loss from suppressing guarded focus_target invocation",
        "the 2026-08-26 real Browser application lifecycle observed remote_desktop.focus_target after the epoch fix and received the typed target_focus_permission_missing host result; this proves the authority request reaches Runtime but still does not prove an OS input effect",
        "the 2026-08-29 Linux window and application Browser executions correctly projected effective input_scope=view_only with target_scoped_keyboard_pointer_dispatch_unsafe; their capture/media passes are not counted as target-local OS-input passes",
        "the 2026-08-29 macOS signed-helper run observed Accessibility granted but Screen & System Audio Recording denied, so it could not reach an independently observed capture-plus-input effect",
        "no real Windows SendInput/UIPI evidence was produced on 2026-08-29"
      ],
      "required_evidence_before_product_complete": [
        "live remoteapp-input-injection-e2e.sh artifact",
        "focus validation E2E",
        "coordinate mapping E2E",
        "target epoch checks on execution path",
        "OS permission checks",
        "monotonic client sequence application",
        "stable input_event_id binding between applied input and OS effect",
        "stale/replayed client sequence rejection before host application",
        "latency measurements",
        "successful input injection E2E",
        "real Windows SendInput pointer/key OS-effect E2E including UIPI denial",
        "real Linux X11/XTest pointer/key OS-effect E2E",
        "signed macOS, Windows, and Linux release evidence for the target-snapshot helper process, including bounded IPC, restart generation, circuit breaker, parent-death cleanup, and repeated-hang/no-zombie checks",
        "prove the implemented Windows generation-owned named-mapping/notification media-host path on a real interactive Windows desktop, including capture, RTP rendering, crash cleanup and target invalidation",
        "Linux X11 Window/Application input remains view_only until a target-bound press/release session replaces desktop-global XTest semantics",
        "Linux X11 Window/Application restart recovery remains fail-closed until a DestroyNotify-backed window-generation lease prevents same-process XID reuse",
        "Linux Wayland portal RemoteDesktop implementation and OS-effect E2E or an explicit product-level unsupported decision",
        "live target_local window and application input artifacts with fresh pre-dispatch target guard evidence",
        "observed OS pointer/key effects from a platform observer independent from the injection path",
        "OS effect timestamps after host_applied_at_ms",
        "OS effect binding to selected Resource URA, session id, input_event_id, target_geometry_revision, and target_focus_epoch",
        "pointer expected/observed position within bounded tolerance",
        "black-gap and foreign-window-occlusion probes produce typed rejection and no independently observed OS effect",
        "keyboard focus/resource binding to the selected target",
        "visible terminal receipt"
      ],
      "non_claims": [
        "view_only safety does not prove interactive input",
        "client_sequence replay rejection does not prove successful OS injection",
        "daemon latency telemetry does not prove observed OS pointer/key effects",
        "runtime permission block projection does not prove live macOS permission revocation E2E",
        "input readiness and policy gates do not prove successful OS injection",
        "the input injection verifier self-test does not prove live host input control",
        "a permission-gated host-runner failure does not prove pointer or keyboard injection",
        "Windows/Linux source and cross-compilation evidence do not prove live OS effects"
      ]
    },
    {
      "id": "audio_video_adaptation",
      "requirement": "Audio/video codec, frame rate, bitrate adaptation, and drop policy are product-ready",
      "status": "partial",
      "current_evidence": [
        "macOS ScreenCaptureKit/VideoToolbox H.264 and filter-scoped ScreenCaptureKit audio run inside the canonical easynet-remoteapp-media-host; H.264 reaches daemon-owned WebRTC through fixed RVID plus raw Annex-B and audio through fixed RAUD plus 20 ms Opus, with independent bounded freshest-data writers and no per-frame JSON/base64",
        "direct WebRTC parses the browser's RFC 6184 Baseline packetization-mode=1 receive format before peer construction; profile-level-id/max-recv-level plus max-fs/max-mbps/max-br produce one constraint for capture resolution, effective FPS, bitrate, and OpenH264 SPS level; a Level 3.1 1080p60/50 Mbps request becomes 720p30/14 Mbps, and an encoded-frame test proves SPS profile_idc=66 and level_idc=31",
        "VideoToolbox descriptor",
        "adaptive bitrate helper",
        "queue/drop boundary tests",
        "host-audio capability truth is bound to the canonical active media-host: macOS ScreenCaptureKit audio may advertise Opus, while Windows and Linux report active_media_session_audio_unavailable and reject an audio offer before SDP answer commit because their hosted session adapters cannot emit validator-checked Opus",
        "host-audio capability discovery executes through the canonical easynet-remoteapp-media-host remoteapp_media_host_v1 mode; daemon admission commits only a strictly validated result after successful child exit, and timeout or malformed protocol kills and reaps the child",
        "device capability projection exposes media_pipeline_support with audio-video scope when the native macOS backend is ready, H.264/Opus payload metadata, bounded queue stale-frame drop policy, native adaptation policy, diagnostic stale-frame policy, and missing media-adaptation E2E as a product blocker",
        "frontend WebRTC setup requests recvonly audio, combines remote audio/video tracks into one presentation stream, exposes autoplay recovery, and surfaces daemon/browser audio evidence",
        "frontend session details surface media bitrate, FPS, drops, and backpressure from daemon/browser media stats",
        "ABI v8 keeps the owned fixed-layout RuntimeInvocationStreamFrameV8 raw-stream representation, while additive ABI v9 exposes explicitly retained/released raw payload leases with the same canonical lifecycle metadata; neither extension changes Invocation authority, sequence, receipt, error, or terminal semantics",
        "release packaging and install gates ship exact v8 and v9 additive export allowlists beside the frozen base v7 ABI contract",
        "Python and Go enable ABI v9 leased streams only when feature discovery and the complete open/retain/release symbol set agree; the ordinary owned stream remains v8, v7 compatibility remains explicit, and malformed partial negotiation fails closed instead of silently changing payload ownership",
        "the standalone ABI v8/v9 specs and mutation gates are wired into CI and SDK cutover, while RemoteApp interactive media remains on WebRTC with private fixed RVID/RAUD media-host lanes instead of creating a second server-stream media tunnel",
        "the 2026-08-26 opt-in EasyRemote live smoke selected ABI v8 through SDK feature discovery and preserved three typed frames byte-for-byte, including arbitrary 16 KiB bytes, embedded NUL bytes, and an empty payload",
        "check-remoteapp-main-crate-implementation-tests.sh runs the main-crate WebRTC app/window display-fallback rejection and native plugin catalogue implementation tests",
        "the canonical media-host emits H.264 and macOS 48 kHz stereo Opus through generation-owned shared mappings; it performs one bounded copy into a preallocated slot and sends only a 56-byte notification, then the daemon performs one bounded detach copy into pooled transport memory at the process-isolation lease to WebRTC/NACK-history ownership boundary so retransmission cannot pin the producer's finite shared ring; RTP/SRTP packetization may copy again, and live signed-host A/V evidence remains pending",
        "the hosted WebRTC bridge restores the full remoteapp_media_pipeline_stats_v1 projection after process isolation, including exact Resource/session/transport/source/pipeline identity, measured FPS and bitrate, bounded latency/drop/queue facts, operational versus observed audio, and applied adaptation events",
        "retired daemon-local Windows WASAPI and Linux PipeWire capture code is not selected by production WebRTC; those platforms remain video-only until equivalent media-host Opus adapters and live isolation evidence exist",
        "native audio RTP writes run in the session-owned abortable transport worker; capture and encoded-packet queues are hard-bounded, overflow drops the oldest audio, and fixed-size shared counters avoid an unbounded per-packet outcome queue",
        "native WebRTC media loop emits remoteapp_media_pipeline_stats_v1 stats bound to selected Resource URA, session id, transport epoch, media source epoch, media pipeline id, negotiated H.264/Opus payload metadata, WebRTC transport, requested/effective/measured FPS, target/observed bitrate, bounded video/audio queue and backpressure counters, audio pipeline readiness, separately observed audio media, and adaptation events",
        "remoteapp-media-adaptation-e2e.sh verifier requires real audio/video artifacts with cross-scenario degraded target/observed bitrate delta, FPS/drop delta, backpressure frame-drop delta versus baseline, same target/pipeline/codec comparability, adaptation events bound to the scenario Resource/session/pipeline, impairment-time ordering, and decoded media render-probe payload evidence after adaptation events",
        "host-remoteapp-media-adaptation-e2e.sh executes the real EasyNet Browser lifecycle for baseline, degraded-network, and backpressure, requires explicit fixture apply/reset, resets again after failure, retains raw scenario artifacts, redacts fixture commands, and delegates matrix projection to the canonical aggregator",
        "remote_desktop.attach plugin manifest and compiled registration declare metadata_json_plus_binary bidi framing instead of JSON-only framing, while preserving canonical Invocation/session metadata outside raw media payloads",
        "the 2026-08-26 live Docker synthetic-media run passed 45/45 stream/bidi assertions, including consumer-drop cancel, provider release, reacquire, unique verified receipt chains, single terminal receipts, and plugin removal; catalog owner_ura, rather than the Device host, is now the asserted callee source of truth"
      ],
      "required_evidence_before_product_complete": [
        "live remoteapp-media-adaptation-e2e.sh artifact",
        "end-to-end video codec negotiation report",
        "payload content type and transport report",
        "host audio path report",
        "requested/effective/measured FPS report",
        "target and observed bitrate report",
        "frame-rate/bitrate soak under load",
        "baseline/degraded-network/backpressure scenarios",
        "same selected Resource URA across media scenarios",
        "same media_pipeline_id, video codec/transport, and audio codec across media scenarios",
        "adaptation events bound to the selected Resource URA, session id, and media pipeline id",
        "degraded-network/backpressure events observed after impairment_applied_at_ms",
        "bounded queue depth and explicit stale-frame drop policy",
        "audio transport-write isolation, hard queue bound, observed queue depth, stale-packet drop policy, internally consistent audio drop counters in every real media scenario, and a positive backpressure-versus-baseline audio drop delta",
        "degraded network adaptation/drop policy E2E",
        "decoded video/audio render probe bound to selected Resource URA, session id, media pipeline id, video codec, video transport, and audio codec",
        "video/audio payload fingerprints from the decoded media render probe",
        "render probe observed after adaptation events for degraded-network/backpressure scenarios",
        "visible terminal receipt"
      ],
      "non_claims": [
        "implemented host-audio code and passing unit tests do not prove decoded host audio on a second device",
        "Windows/Linux host audio remains unsupported rather than inferred from available OS primitives; implementation and decoded isolation evidence are both still required",
        "audio pipeline readiness does not imply audio media was observed; product evidence must record positive decoded packets or samples",
        "bounded audio writer source and unit tests do not prove behavior under a live slow or disconnected receiver",
        "synthetic media carrier does not prove host audio/video product readiness",
        "the media adaptation verifier self-test does not prove live media product behavior"
      ]
    },
    {
      "id": "multi_window_tracking",
      "requirement": "Multi-window/multi-application independent tracking works as an execution effect",
      "status": "partial",
      "current_evidence": [
        "target tracker state machine",
        "move/resize/loss/rebind events",
        "cross-display application window-set rebind",
        "application capture creates streams only for owner-matched committed windows",
        "bounded macOS multi-surface compositor preserves black gaps and deterministic z-order",
        "frontend session details surface target tracking recovery state instead of opaque failure",
        "frontend exposes a Refresh targets CTA for target tracking recovery instead of leaving lost targets as a dead end",
        "check-remoteapp-main-crate-implementation-tests.sh runs main-crate app/window target rebind implementation tests",
        "remoteapp-multi-window-tracking-e2e.sh verifier for real independent app/window tracking artifacts with per-stream decoded-frame probes bound to each selected Resource/session/stream/frame source/media epoch/sentinel"
      ],
      "required_evidence_before_product_complete": [
        "live remoteapp-multi-window-tracking-e2e.sh artifact",
        "independent concurrent window streams",
        "distinct Resource URAs, session ids, stream ids, media source epochs, and frame source ids",
        "decoded-frame probe bound to each independent stream's selected Resource URA, session id, stream id, frame source id, media source epoch, and selected sentinel id/hash",
        "non-interleaved frames for independently tracked streams",
        "selected target sentinel rendering for each independently tracked stream",
        "no foreign or cross-stream sentinel leakage between independently tracked streams in stream summaries or decoded-frame probes",
        "move/resize geometry churn with increasing target_geometry_revision",
        "cross-display application window-set churn with pending media rebind and TARGET_REBOUND",
        "committed window-set sentinel rendering after application rebind",
        "uncommitted same-app window sentinel absence after application rebind",
        "target loss with bounded rebind or explicit rebind failure",
        "live decoded multi-display MultiAppSurface execution evidence without display fallback",
        "selected Resource URA session subjects",
        "visible terminal receipt"
      ],
      "non_claims": [
        "data structures alone do not prove independent tracking execution",
        "distinct stream identifiers alone do not prove each stream rendered its selected window/application target",
        "target tracker unit tests do not prove real stream isolation",
        "the multi-window tracking verifier self-test does not prove live app/window churn behavior"
      ]
    },
    {
      "id": "session_recovery_lifecycle",
      "requirement": "Disconnect/reconnect, session resume, consent revoke, cancel, timeout are complete",
      "status": "partial",
      "current_evidence": [
        "lease monitor",
        "refresh/end session",
        "explicit end_session and lease-timeout terminal_receipt projection",
        "host-remoteapp-session-timeout-e2e.sh creates a short-lived session through the CLI, observes session_expired through public show_session, and verifies post-timeout end_session idempotency preserves the original terminal receipt",
        "host-remoteapp-session-cancel-e2e.sh creates a live-target session through the CLI, invokes public remote_desktop.end_session with user_cancelled, observes the closed state through public show_session, and verifies repeated end_session preserves the original cancel terminal receipt",
        "host-remoteapp-permission-revoke-e2e.sh creates a live-target session and waits for real platform permission revoke evidence before accepting public show_session projection of target_permission_revoked, revoked consent, ordered TARGET_PERMISSION_REVOKED/MEDIA_SOURCE_LOST/SESSION_CLOSED events, and terminal receipt binding",
        "host-remoteapp-session-resume-e2e.sh creates a short-lease session, invokes public remote_desktop.refresh_lease, waits past the original lease, validates the same non-terminal session through public show_session, and closes it with resume_e2e_cleanup",
        "host-remoteapp-session-resume-e2e.sh is lease-refresh survival evidence only and cannot satisfy browser transport resume",
        "frontend-remoteapp-browser-lifecycle-e2e.sh has an opt-in real browser transport-resume path requiring external disconnect/reconnect commands and same-session survival, old PeerConnection retirement, a newer daemon-issued transport epoch, a new connected PeerConnection, watch_events reattachment, decoded media after resume, and unchanged input authority",
        "historical 2026-08-23 record: local macOS window lifecycle artifacts were reported passed at target/e2e/host-remoteapp-session-timeout/20260823-live-window-causal-222646-11519/report.md, target/e2e/host-remoteapp-session-cancel/20260823-live-window-causal-222700-12564/report.md, and target/e2e/host-remoteapp-session-resume/20260823-live-window-stable-222830-19233/report.md; these artifacts are not present in the current checkout",
        "historical 2026-08-23 record: local macOS application lifecycle artifacts were reported passed at target/e2e/host-remoteapp-session-timeout/20260823-live-application-causal-222846-20408/report.md, target/e2e/host-remoteapp-session-cancel/20260823-live-application-causal-222859-21255/report.md, and target/e2e/host-remoteapp-session-resume/20260823-live-application-stable-222859-21261/report.md; these artifacts are not present in the current checkout",
        "frontend retains closed end_session views with terminal_receipt while clearing session tokens",
        "permission revocation terminates the daemon session with a terminal_receipt and frontend terminal sync",
        "permission observation now uses a durable fail-closed confirmation state: one negative host sample pauses media/input without revoking consent or closing the session, positive host evidence clears verification, and only a repeated negative sample confirms target_permission_revoked",
        "permission_verification_pending survives recovery snapshots and daemon rehydration; Rust aggregate tests prove restoration preserves the same session/consent while confirmed revocation still reaches the reason-bound terminal receipt path",
        "after rebuilding and restarting the current daemon, three consecutive unchanged 2026-08-26 macOS EasyNet-relay Browser runs v6/v7/v8 passed relay-only selected-pair, two-or-more rendered-frame, terminal-receipt, lease-release, and environment-restoration checks without the prior transient permission terminalization",
        "target loss and transport failure taxonomy",
        "frontend watch_events degraded/revoked recovery handling",
        "frontend exposes Retry session for daemon retry_session guidance and allows create_session after retained terminal receipts",
        "frontend preserves non-terminal RemoteApp sessions across device-offline presence drops, validates with remote_desktop.show_session on resume, and rebinds WebRTC without ending the daemon session on failed rebind",
        "frontend treats failure of an already-connected PeerConnection as replaceable transport infrastructure rather than ending the RemoteApp session, retaining subject, consent, lease, event watch, and input authority while negotiating a fresh transport epoch",
        "automatic transport replacement uses a browser-owned three-attempt increasing-backoff state machine, is cancelled by terminal/offline/reset lifecycle transitions, resets only after presented media is acknowledged, and preserves the session for explicit Retry session after exhaustion",
        "a transient disconnected PeerConnection receives a five-second recovery grace; returning connected cancels replacement, while grace expiry enters the same bounded transport-replacement state machine",
        "the 2026-08-26 live Browser window restart-resume artifact passed across a real paired daemon stop/start: Device state moved ONLINE/J700 to UNKNOWN/C440 to ONLINE/J700, session rdp-6a0f55cef39688993fba6f06 remained non-terminal, the old PeerConnection reached closed/closed, transport epoch increased from 1787686710123091 to 1787686896984117, a replacement connected/connected PeerConnection and watch_events subscription attached, and one 1688x1080 frame rendered after resume",
        "the same restart-resume artifact preserved input authority with an explicit Accessibility policy block before and after restart, then ended the same session with a visible caller_ended terminal receipt",
        "the reusable 2026-08-26 host-remoteapp-daemon-sigkill-e2e.sh live artifact killed exact path-verified target/debug/easynet-daemon PID 4365 during active session rdp-b8606004250fbe8de38e6340, restarted path-verified PID 53848 to J800, observed ONLINE/J700 to UNKNOWN/C440 to ONLINE/J700, recovered the same session with a replacement PeerConnection, newer transport epoch and watch_events subscription, rendered a frame after recovery, and caller_ended closed the recovered session",
        "the same live run recorded both public Unix endpoints before, during, and after the crash: the dead control and Invocation listener inodes remained present but rejected connections, then daemon-owned bind replaced each with a distinct reachable inode without wrapper-side or manual cleanup",
        "the reusable 2026-08-26 host-remoteapp-terminal-receipt-crash-e2e.sh live artifact crashed exact daemon PID 91393 after durable terminal promotion and before the end_session response, restarted PID 92151 to J800, preserved the same Browser session through public show_session, and retained one exact caller_ended terminal receipt across crash marker, recovery snapshot, paired-user signed public show_session, and repeated public end_session",
        "the close-crash replay runner uses the real product caller_ended reason, an E2E-feature-only post-promotion crash point, and an owner-only one-shot exact-session arm; ordinary product builds contain no callable crash surface",
        "the reusable 2026-08-26 host-remoteapp-target-monitor-worker-recovery-e2e.sh live artifact crashed only target-monitor generation 1 for Browser session rdp-66843994a4396d038cc76b94 while exact feature daemon PID 62280 remained J800; Browser public events and the durable recovery snapshot both preserved PLUGIN_WORKER_CRASHED, PLUGIN_WORKER_RESTARTED, TARGET_MONITOR_RESTARTED order and replacement generation 2",
        "the worker-only artifact preserved the selected Resource, public session, consent epoch, target binding epoch, WebRTC transport epoch, and media-source epoch, rendered a later Browser frame without new consent, and ended the same session with a visible caller_ended receipt; ordinary builds contain no worker-crash surface",
        "the 2026-08-29 Linux EasyNet-relay refresh/resume run at target/e2e/remoteapp-easynet-relay/live-linux-display-refresh-resume-20260829-r5/report.json passed real Hub lease rotation, same-session daemon restart recovery, replacement connected transport, watch reattachment, nine rendered frames, and terminal cleanup",
        "canonical SDK cancel/timeout semantics",
        "remoteapp-crash-restart-recovery-e2e.sh verifier for real daemon/plugin crash and restart recovery artifacts with ordered lifecycle events bound to the selected Resource URA and session id",
        "2026-08-23 live daemon kill/restart probe returned session_not_found for rd-crash-probe-45956, proving the historical runtime did not rehydrate active RemoteApp sessions after daemon crash",
        "RemoteDesktopRecoveryStore and RemoteDesktopRecoverySnapshot now define the daemon-local durable snapshot contract with round-trip and corrupt-snapshot fail-closed tests",
        "plugin startup now rehydrates snapshots into degraded RemoteDesktopSession rows and preserves session_id, selected Resource URA, caller, consent receipt, daemon-local session token, target binding, event replay, and terminal receipt",
        "recovery batch loading reports and skips corrupt snapshot rows without dropping valid rows",
        "Unix recovery directories and snapshot files are private because persisted snapshots include daemon-local session tokens",
        "rehydrated non-terminal sessions can start a fresh media negotiation epoch without minting a new session id",
        "rehydrated non-terminal sessions re-enter target monitoring after plugin startup",
        "target monitor desired tracking state is owned outside the worker thread so worker replacement can re-seed tracked sessions",
        "target monitor supervisor and generation workers retain a focused component context instead of the RemoteDesktopPlugin aggregate, preventing plugin Drop from participating in a circular supervisor/generation join",
        "recovery startup synchronously settles snapshots whose lease elapsed while the daemon was down as durable session_expired terminal rows",
        "recovery snapshots persist session-local runtime input permission blockers and public show_session projects them after plugin restart",
        "runtime regression coverage proves public show_session, watch_events, and end_session operate on a rehydrated session row",
        "frontend retry_session handling now preserves the daemon session id, token, consent, selected Resource URA, and event history while replacing only the WebRTC transport with a strictly newer epoch",
        "frontend source gates reject the retired end_session-plus-create_session retry flow and require same-session device-resume and explicit-retry coverage",
        "frontend retry uses a monotonic generation fence and PeerConnection ownership checks so offline/end/reset cannot be overwritten by stale show_session or set_description continuations",
        "recovery snapshots have a 4 MiB read/write bound enforced before JSON decode and during serialization, and one store-global lock serializes commit/load/delete",
        "terminal rows pruned from the in-memory session aggregate are now deleted from the recovery store instead of being re-persisted as unbounded disk tombstones",
        "startup recovery derives its 640-row default ceiling from 128 active plus four terminal rows per active session, caps all directory entries including sidecars, and rejects batches above 64 MiB before JSON decode",
        "direct WebRTC endpoints, pending setup reservations, partially-created peer cleanup, and diagnostic preview task groups now converge on one process-owned round-robin settlement executor",
        "bounded settlement timeouts retain completion receivers and JoinHandle ownership; explicit negative receipts drain remaining owners before reporting Failed, and panicking jobs remain quarantined after manager handles disappear",
        "endpoint admission is sealed and generation-fenced during terminal teardown, and setup cleanup remains manager-visible until a real peer-close receipt arrives",
        "a setup reservation dropped before terminal teardown keeps its explicit negative receipt manager-visible, so unwind cannot erase an unknown partially-created peer",
        "PeerConnection callbacks retain the transport manager weakly and media workers retain only an independent Tokio runtime handle, preventing manager/peer/worker ownership cycles",
        "explicit end, lease expiry, and permission revocation, including zero-transport cases, use one terminal finalizer that persists a private terminal candidate before publishing in-memory Closed",
        "terminal persistence faults retain in-memory Closing and remain queued with bounded exponential backoff rather than being reported as settled",
        "terminal candidates are durably staged outside the global session-store mutex but remain non-authoritative until a short revision CAS atomically promotes the exact staged body, so a crash after a stale candidate cannot recover an obsolete Closed snapshot",
        "typed settlement quarantine records job/session/failure/projection state, retries outcome projection with bounded backoff, exposes unhealthy transport_settlement_health, and fail-closes new session admission while resource ownership remains unproven",
        "a quarantined session termination publishes a durable terminal Failed receipt instead of remaining anonymously in Closing, while the process continues retaining the unproven resource owner for controlled runtime recycle"
      ],
      "required_evidence_before_product_complete": [
        "long-outage and network transport reconnect handoff E2E",
        "live consent revoke termination E2E pass report from a real host permission revoke",
        "canonical Axon cancel/timeout receipt-chain evidence for RemoteApp abilities",
        "live remoteapp-crash-restart-recovery-e2e.sh artifact",
        "recovered WAL/idempotency/replay-guard/lock state",
        "Windows named-pipe restart lifecycle evidence if Windows remains a supported RemoteApp daemon platform"
      ],
      "non_claims": [
        "taxonomy and unit coverage do not prove recovery product behavior",
        "lease refresh and same-session show_session survival do not prove WebRTC media or input transport recovery",
        "the browser transport-resume verifier self-test does not prove a live network disconnect/reconnect",
        "the crash/restart recovery verifier self-test does not prove live process recovery",
        "the live daemon SIGKILL, close-crash receipt-replay, and macOS/window target-monitor worker-only proofs do not prove Windows named-pipe restart or cross-device recovery",
        "Stage 1 recovery tests plus one orderly restart, one active-session SIGKILL/Unix-socket proof, one post-promotion close-crash proof, and one target-monitor generation proof do not prove the remaining full crash/restart matrix",
        "typed quarantine and signed-campaign gates prevent false success claims but do not themselves prove a real host transport failure recovered or a real cross-device campaign executed"
      ]
    },
    {
      "id": "network_fallback",
      "requirement": "NAT/relay/WebRTC/direct fallback network paths are verified",
      "status": "partial",
      "current_evidence": [
        "typed host/STUN/TURN/EasyNet relay route evidence",
        "source-level provider gates",
        "daemon transport view projects browser client_ice_servers",
        "frontend RTCPeerConnection consumes session-projected ICE servers",
        "frontend session details render daemon route state such as host_only with no NAT/relay",
        "native WebRTC media stats project selected candidate-pair local_candidate_type, remote_candidate_type, selected_route_class, and protocol without exposing addresses or credentials",
        "remoteapp-network-fallback-e2e.sh verifier requires real network fallback artifacts to carry nominated/selected/succeeded candidate-pair evidence with selected_route_class matching direct, STUN/reflexive, and relay route expectations",
        "network fallback evidence identity now matches the production Browser path: caller_ura is an admitted User/Agent/Authority, callee_ura is the Remote Desktop SystemAgent, provider_device_ura is the execution host, and client_endpoint_id identifies the Browser/network peer without promoting it to a Device",
        "network fallback verifier requires production create_session/set_description/watch_events/report_client_state/end_session evidence and rejects diagnostic attach as a substitute for WebRTC signalling",
        "the bundled Browser runner reads the exact selected candidate pair from RTCPeerConnection.getStats before its rendered-frame observation and records stable pair/local/remote ids, candidate types, protocol, nomination/state, byte counters, RTT, and redacted Invocation identities",
        "network fallback verifier requires applied network fixture constraints, allowed/blocked route classes, selected-pair observation after constraints, and rendered media bound to the selected Resource/session/route/candidate pair after selected-pair observation",
        "the 2026-08-26 host-remoteapp-direct-e2e.sh live child passed with every daemon STUN/TURN/EasyNet relay variable removed, zero projected ICE URLs, host-only local/remote SDP, a connected/succeeded/nominated UDP direct pair, positive bidirectional bytes, three post-selection presented frames, correct User/SystemAgent/Device identity separation, a caller_ended terminal receipt, and ordinary Runtime restoration",
        "host-remoteapp-turn-relay-e2e.sh builds a content-addressed coturn fixture, runs the Browser with relay-only ICE, requires server-observed relay allocation, emits a focused child proof, and restores the ordinary daemon configuration",
        "the 2026-08-26 live TURN child proof passed with three server-observed allocations, relay-only local SDP, connected/succeeded/nominated ICE, positive bidirectional bytes, three post-selection presented frames, correct User/SystemAgent/Device identity separation, and a caller_ended terminal receipt",
        "host-remoteapp-stun-srflx-e2e.sh now rejects macOS Docker Desktop's non-routable hidden-VM NAT, runs an address-redacted RFC 5389 Binding observer on the provider host, requires an externally reachable VM-NAT Browser context, projects only a redacted STUN URL through the real daemon, admits only srflx/prflx Browser outbound while retaining host/srflx/prflx provider inbound return candidates, records directional accepted/rejected counters, requires a server-observed binding ordered before pair selection, enforces an outer deadline, validates later media and terminal cleanup, and restores the ordinary daemon; the observer passed an independent coturn turnutils_stunclient interoperability probe with a real VM-NAT reflexive mapping, but no complete live Browser route is claimed",
        "the 2026-08-26 host-remoteapp-easynet-relay-e2e.sh live child passed with a Hub-authenticated session/resource-bound five-minute lease, no static daemon TURN credentials, three independently observed coturn allocations, a selected/nominated/succeeded relay pair, three post-selection rendered frames, redacted ephemeral-auth evidence, a caller_ended terminal receipt followed by HTTP 409 same-binding reacquire rejection from the Hub release tombstone, and ordinary Hub/daemon restoration",
        "the 2026-08-29 Linux direct application child passed at target/e2e/remoteapp-direct/live-linux-application-docker-browser-20260829-r3/report.json with host-only selection, 14 rendered frames, connected ICE, and live_runner provenance",
        "the 2026-08-29 Linux TURN children passed both window and application at target/e2e/remoteapp-turn-relay/live-linux-window-20260829-r2/report.json and target/e2e/remoteapp-turn-relay/live-linux-application-20260829-r1/report.json; each used relay-only Browser policy/SDP plus server-observed allocation evidence, connected ICE, rendered media, and terminal cleanup",
        "the 2026-08-29 Linux EasyNet-relay children passed both window and exact two-window process-scoped application at target/e2e/remoteapp-easynet-relay/live-linux-window-20260829-r4/report.json and target/e2e/remoteapp-easynet-relay/live-linux-application-20260829-r3/report.json; the Browser selected relay-class paths, rendered 12 and 7 frames respectively, and closed normally",
        "the 2026-08-29 accelerated EasyNet lease refresh plus daemon resume child passed at target/e2e/remoteapp-easynet-relay/live-linux-display-refresh-resume-20260829-r5/report.json with relay_lease_refresh_resume=true, nine rendered frames, a connected replacement transport, and the same public session",
        "two 2026-08-29 isolated STUN attempts reached the real binding observer but timed out before ICE connection because the Browser VM and provider could not establish a viable private return route; target/e2e/remoteapp-stun-srflx/live-linux-application-colima-20260829-r11/report.json and live-linux-application-reverse-vm-20260829-r12/report.json remain failed artifacts",
        "relay refresh ownership is explicit across daemon/Hub boundaries: Hub tests prove a refreshed lease supersedes the old ID and only the current ID can terminalize the binding; daemon tests prove successful rotation retains the fresh lease for terminal release, an idempotent duplicate refresh cannot release the session-owned current lease, a refresh that loses its session owner releases the unattached current Hub lease, and an expired failed refresh retires the exact current lease",
        "host-remoteapp-easynet-relay-e2e.sh --refresh-resume now accelerates the real Hub lease TTL, keeps the daemon running until the Browser has observed the daemon-watchdog-issued distinct redacted lease through public show_session, then restarts the paired daemon and requires the Browser to preserve the public session while replacing the PeerConnection and transport epoch, reattaching watch_events, rendering media, and preserving input authority",
        "verify-remoteapp-relay-refresh.py and the canonical network projector/verifier bind initial and refreshed lease IDs to the same session/Resource and replacement transport; unchanged IDs, stale epochs, cross-session evidence, missing resumed media, missing terminal receipts, or credential-bearing evidence fail closed; self-test and skip reports explicitly retain zero live relay coverage"
      ],
      "required_evidence_before_product_complete": [
        "live remoteapp-network-fallback-e2e.sh artifact",
        "real STUN srflx route report",
        "applied network fixture constraints for every route scenario",
        "allowed and blocked route class evidence for every route scenario",
        "selected/nominated/succeeded WebRTC candidate-pair evidence",
        "selected local and remote candidate id evidence",
        "stable selected candidate_pair_id evidence",
        "WebRTC stats bound to selected Resource URA, session id, admitted caller URA, Remote Desktop SystemAgent callee URA, provider Device host URA, Browser/network client endpoint id, and route kind",
        "selected-pair observation after network constraints on every route",
        "rendered media bound to the selected Resource URA, session id, route kind, and candidate_pair_id after selected-pair observation on every route",
        "redacted credential/reachability evidence",
        "live long-session Hub relay lease rotation and same-session replacement-transport artifact from host-remoteapp-easynet-relay-e2e.sh --refresh-resume",
        "visible terminal receipt on every route"
      ],
      "non_claims": [
        "route type source checks do not prove real NAT/relay reachability",
        "route class labels without applied network constraints do not prove fallback behavior",
        "the network fallback verifier self-test does not prove live network reachability",
        "the focused direct, TURN, and EasyNet relay child proofs do not prove STUN srflx fallback",
        "the STUN harness and failed live attempts do not prove a selected, connected srflx/prflx route",
        "the focused EasyNet relay child does not by itself complete the aggregate four-route fallback campaign",
        "relay refresh ownership unit coverage and the accelerated refresh/resume harness self-test do not prove a live long-session credential rotation and subsequent transport replacement"
      ]
    },
    {
      "id": "frontend_lifecycle",
      "requirement": "Frontend UI can discover, authorize, start, display, control, and end session",
      "status": "partial",
      "current_evidence": [
        "frontend subject boundary",
        "component coverage for picker to consent to create to WebRTC attach to watch_events to end",
        "target-scoped WebRTC lifecycle unit coverage",
        "protocol projection coverage for daemon input_readiness driven input gating",
        "frontend protocol/store/UI coverage for daemon terminal_receipt projection after end_session",
        "frontend permission-revoked recovery synchronizes daemon terminal_receipt through show_session",
        "frontend session details render daemon-projected target recovery status/reason/action",
        "frontend session details render daemon-projected H.264/Opus media_pipeline_support, audio/video track evidence, drop policy, and product blockers",
        "frontend action row executes daemon-guided target refresh recovery",
        "frontend action row executes daemon-guided retry_session recovery by preserving the session aggregate and replacing only its WebRTC transport",
        "frontend share picker exposes host-local remote_desktop.permission_status preflight before create_session",
        "frontend denied permission_status stays in the share picker with Request permission recovery instead of becoming a session-level error",
        "frontend projects capture and input as independent explicit permission states; known capture denial or restart-required state blocks Share, double-click, consent, and create_session while known input denial downgrades the request to view_only",
        "the Browser lifecycle verifier accepts an optional permission_requested step only between permission_status_checked and consent_granted, requires granted capture for passing evidence, and enforces host-local subjects for both permission_status and request_permission",
        "frontend protocol/store/UI now project target_focus_epoch, reject stale/missing focus epochs before RTC input send, and send target_focus_epoch on pointer/key frames",
        "product-flow harness entrypoint",
        "historical 2026-08-23 record: local product-flow both report passed Hub API readiness, daemon readiness, frontend typecheck, DeviceMediaAccess UI flow, host target picker freshness, decoded-frame WebRTC, and view-only input safety for window and application targets; its artifact is not present in the current checkout",
        "current-checkout 2026-08-26 live target-picker freshness artifacts separately prove window_id + owner_pid selection and process-scoped application identity + owner pid + two resolved native windows + ordered surface membership + window_set_epoch; product-flow and completion gates require both distinct subreports",
        "frontend-remoteapp-browser-lifecycle-e2e.sh verifier requires real Browser/Tauri lifecycle artifacts to carry UI automation evidence source, monotonic observed timestamps, production remote_desktop.set_description signaling, connected WebRTC state, attached media stream, visible media element, positive rendered frame count, visible input status, terminal receipt, and submitted data-channel frame and daemon applied event target_focus_epoch evidence for input_applied flows",
        "EasyNet Frontend now includes a bounded Playwright/Chrome lifecycle runner that drives the public UI, observes production ability requests without constructing Invocation envelopes, samples the real RTCPeerConnection and decoded video element, correlates input data-channel frames with daemon INPUT_FRAME_APPLIED events, and emits fail-closed verifier evidence",
        "the Browser runner recognizes the Runtime prepare/execute two-response protocol, records structured invocation failures, and fails immediately on a terminal failed receipt instead of reporting a delayed session-DOM timeout",
        "the 2026-08-26 live Browser window artifact passed app load, authentication, target picker, permission_status, consent, create_session, production set_description, connected WebRTC, watch_events, attached audio/video tracks, four rendered frames, media-pipeline UI, explicit Accessibility policy block, end_session, and visible caller_ended terminal receipt",
        "the live Browser Invocation evidence preserves User caller, Remote Desktop SystemAgent callee, Device execution host, selected window Resource subject, descriptor refs, and one session binding across all production abilities",
        "the 2026-08-26 live Browser window restart-resume artifact preserved the same non-terminal session surface while the Device was unavailable, then projected the newer Runtime transport epoch, replacement PeerConnection, watch_events reattachment, rendered media after resume, and visible terminal receipt",
        "the 2026-08-26 live Browser application artifact binds the picker inventory and Runtime execution scope separately: Application Resource, non-display-scoped window set, AppSurface capture, no scope widening/display fallback, production ability tuple, rendered media, focus_target permission result, and visible terminal receipt",
        "the reusable 2026-08-26 host-remoteapp-daemon-sigkill-e2e.sh artifact preserves the active window session surface through an exact-PID ungraceful daemon process death and path-verified restart, proves Unix control/Invocation stale-inode replacement without manual cleanup, then rebinds Runtime/WebRTC/watch/media before visible terminal cleanup"
        ,"the reusable 2026-08-26 host-remoteapp-target-monitor-worker-recovery-e2e.sh artifact drives the real Browser UI through an owner-only exact-session target-monitor generation crash, visibly projects ordered worker recovery without closing the PeerConnection, renders a later frame on the same media source, and ends with a visible terminal receipt",
        "frontend handles TARGET_PERMISSION_VERIFICATION_PENDING by closing local media/input while retaining the same session token and durable watch_events owner; TARGET_PERMISSION_VERIFICATION_CLEARED retries only WebRTC with a newer daemon transport epoch and does not call end_session"
      ],
      "required_evidence_before_product_complete": [
        "live successful input-control Browser path whose submitted data-channel frame and daemon applied event target_focus_epoch match the Runtime session target focus epoch",
        "live Browser lifecycle on every supported desktop platform or an explicit product-level unsupported decision",
        "live cross-device Browser lifecycle using a real remote capture provider rather than a local or synthetic carrier",
        "live Browser coverage for remaining supported platform, input, network, and cross-device scenarios"
      ],
      "non_claims": [
        "component tests do not prove full real frontend product lifecycle",
        "the Browser/Tauri verifier self-test does not prove live frontend product behavior",
        "the 2026-08-24 runner attempt reached no product step because the configured frontend and backend endpoints were not listening; it is failure evidence, not a product pass",
        "the 2026-08-26 Browser passes prove local macOS window and application lifecycles, one orderly daemon restart-resume, one active-session daemon SIGKILL plus Unix stale-socket recovery, one close-crash replay, and one target-monitor worker-only recovery with policy-blocked input; they do not prove successful OS input injection, display target behavior, Windows/Linux behavior, pixel-level unrelated-sentinel exclusion, the remaining crash matrix, degraded media, or a remote-device network path"
      ]
    },
    {
      "id": "cross_device_e2e",
      "requirement": "Cross-device E2E smoke/regression exists beyond local provider boundary",
      "status": "partial",
      "current_evidence": [
        "remoteapp-cross-device-product-smoke.sh composes Docker two-node routing",
        "synthetic media/bidi carrier gate",
        "Service owner multihost projection regression coverage prevents same-user multi-device Pages projection conflicts",
        "cross-device smoke reports source/runtime image provenance so stale-image failures are distinguishable",
        "cross-device smoke report aggregates observed caller/provider device pairs and fails local-provider-only completed runs instead of marking them passed",
        "the 2026-08-26 live combined smoke passed with local_provider_boundary_only=false: 58/58 governed cross-device routing assertions and 45/45 synthetic stream/bidi carrier assertions across distinct caller/provider Device URAs",
        "the same live run records dirty source revision f3a02fc0f91c7495284c838925734dfcd2227f53 and prebuilt runtime image sha256:63dfb06db969e6ee0c3029e9d95baa3772ab358f9327c88632a400863f270703 created on 2026-08-25, so it is intentionally not clean-build release evidence",
        "the synthetic media plugin fixture now uses descriptor schema v3 with explicit task exposure, media dedicated surface, dedicated-surface subject contract, and json_frames bidi wire kind",
        "stream/bidi receipt tuple assertions resolve the expected callee from catalog owner_ura and no longer confuse the provider Device host with the plugin-management Agent owner",
        "remoteapp-cross-device-remoteapp-e2e.sh now requires the production create_session/set_description/watch_events/report_client_state/end_session chain and rejects diagnostic remote_desktop.attach as media evidence",
        "the cross-device RemoteApp verifier uses the production Browser identity tuple: admitted caller_ura, SystemAgent callee_ura, provider_device_ura execution host, and opaque client_endpoint_id; it does not promote the Browser peer to a Device",
        "the cross-device RemoteApp verifier requires a remote execution boundary, connected PeerConnection and ICE state, a selected candidate-pair id, H.264 production media rendered on the bound Browser endpoint after connection, and a canonical verified terminal receipt for display/window/application",
        "host-local decoded-frame scripts cover local capture/render decode"
      ],
      "required_evidence_before_product_complete": [
        "RemoteApp-specific two-device or network namespace E2E",
        "an observed Browser client endpoint and provider execution Device boundary with local_provider_boundary_only=false",
        "remote target inventory",
        "remote production WebRTC/media from actual display/window/application capture using set_description and report_client_state, never diagnostic attach",
        "connected PeerConnection and ICE state with a selected candidate-pair id",
        "H.264 production media rendered on the bound Browser client endpoint only after connection",
        "input policy",
        "canonical verified terminal receipt and teardown evidence"
      ],
      "non_claims": [
        "cross-device routing and synthetic media do not prove real RemoteApp product behavior",
        "same-device or local-provider-only reports are not cross-device RemoteApp evidence"
      ]
    }
  ]
}
