# JSON Control Caller Inventory

This path is kept only as a navigation shim.

The canonical migration audit is [`../json-control-caller-inventory.md`](../json-control-caller-inventory.md).
Do not duplicate inventory tables here; JSON-control demotion status must remain single-source.
