schema_version = "1"
name = "policy.request.create"
description = "Create or return an idempotent pending RFC-014 PermissionRequest."

[input_schema]
type = "object"
required = ["request"]
additionalProperties = false

[input_schema.properties.actor_ura]
type = "string"

[input_schema.properties.request]
type = "object"
