schema_version = "2"
name = "identity.list_user_pubkeys"
descriptor_version = "1.0.0"
description = "List trusted public keys currently bound to a user URA."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["agent_ura"]
additionalProperties = false

[input_schema.properties.agent_ura]
type = "string"
description = "User URA whose trusted keys should be listed."
