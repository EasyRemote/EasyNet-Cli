schema_version = "3"
name = "meta.list_abilities"
descriptor_version = "1.0.0"
description = "Return the full local AbilityDescriptor catalogue. Canonical Invoke surface for ability discovery; the MCP-shaped projection lives at mcp.bridge.list_tools for external MCP clients."
exposure = "operator"
dedicated_surface = "none"
subject_contract_kind = "authenticated-user"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false

[input_schema.properties.agent_ura]
type = "string"
description = "Canonical owner URA. Filters the catalogue to abilities published by that owner."

[input_schema.properties.scope]
type = "string"
enum = ["local", "realm"]
description = "`local` (default) returns this runtime's SystemAgent- and Service-owned abilities. `realm` adds realm Authority-published abilities the realm Authority broadcast at join + heartbeat (RFC-001 v4.1.7)."

[input_schema.properties.subject_ura]
type = "string"
description = "Owner URA or full Ability URA. Owner URAs filter by publisher; Ability URAs filter to one canonical ability."
