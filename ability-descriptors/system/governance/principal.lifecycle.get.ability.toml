schema_version = "3"
name = "principal.lifecycle.get"
descriptor_version = "1.0.0"
description = "Read one daemon runtime principal lifecycle record."
exposure = "internal"
dedicated_surface = "none"
subject_contract_kind = "route-target"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["principal_ura"]
additionalProperties = false

[input_schema.properties.principal_ura]
type = "string"
description = "Principal URA to read."
