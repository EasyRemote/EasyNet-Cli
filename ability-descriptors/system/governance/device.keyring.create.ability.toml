schema_version = "2"
name = "device.keyring.create"
descriptor_version = "1.0.0"
description = "Create a managed Ed25519 signing key in the local keyring-management SystemAgent vault and return its public projection."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "manage"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["purpose"]
additionalProperties = false

[input_schema.properties.bound_subject]
type = "string"
minLength = 1

[input_schema.properties.purpose]
type = "string"
minLength = 1
