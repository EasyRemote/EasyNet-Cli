schema_version = "3"
name = "admission.explain"
descriptor_version = "1.0.0"
description = "Return an observer-scoped RFC-014 admission diagnostic projection."
exposure = "internal"
dedicated_surface = "none"
subject_contract_kind = "route-target"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["observer_ura"]
additionalProperties = false
oneOf = [{ required = ["invocation_id"] }, { required = ["request_id"] }, { required = ["trace_id"] }, { required = ["root_id"] }]

[input_schema.properties.invocation_id]
type = "string"

[input_schema.properties.observer_ura]
type = "string"

[input_schema.properties.request_id]
type = "string"

[input_schema.properties.root_id]
type = "string"

[input_schema.properties.trace_id]
type = "string"
