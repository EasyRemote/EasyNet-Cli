schema_version = "2"
name = "device.keyring.rotate"
descriptor_version = "1.0.0"
description = "Retire an active managed signing key and mint its next epoch successor in the keyring vault."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "manage"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["key_id"]
additionalProperties = false

[input_schema.properties.key_id]
type = "string"
minLength = 1
