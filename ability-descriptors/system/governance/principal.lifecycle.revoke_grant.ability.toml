schema_version = "2"
name = "principal.lifecycle.revoke_grant"
descriptor_version = "1.0.0"
description = "Revoke an authorization grant from a daemon runtime principal."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "manage"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":true,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["request"]
additionalProperties = true

[input_schema.properties.request]
type = "object"
description = "Product-neutral principal lifecycle command envelope."
