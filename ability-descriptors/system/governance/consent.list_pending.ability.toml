schema_version = "2"
name = "consent.list_pending"
descriptor_version = "1.0.0"
description = "Snapshot the approval-broker's pending queue. Unary sibling of consent.subscribe — same data, no live tail; suitable for poll callers that don't want to wire stream handling."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false
