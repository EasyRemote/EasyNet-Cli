schema_version = "3"
name = "device.keyring.expire_set"
descriptor_version = "1.0.0"
description = "Set or update the expiry timestamp for a managed signing key."
exposure = "internal"
dedicated_surface = "none"
subject_contract_kind = "route-target"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "manage"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["key_id", "expires_unix_ms"]
additionalProperties = false

[input_schema.properties.expires_unix_ms]
type = "integer"

[input_schema.properties.key_id]
type = "string"
minLength = 1
