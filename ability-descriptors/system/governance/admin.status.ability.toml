schema_version = "3"
name = "admin.status"
descriptor_version = "1.0.0"
description = "Operator-facing component snapshot: build version, membership state, ability count, hosted-Agent count. v1 is a cheap rollup for `easynet status` dashboards; live probes land later."
exposure = "operator"
dedicated_surface = "none"
subject_contract_kind = "route-target"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false
