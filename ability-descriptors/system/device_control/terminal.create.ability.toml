schema_version = "2"
name = "terminal.create"
descriptor_version = "1.0.0"
description = "Create an interactive PTY session and return its opaque session_id. Pair with terminal.attach (data plane) and terminal.close (lifecycle teardown). Part of the baseline-locomotion-v1 profile."
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "stream"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false

[input_schema.properties.cols]
type = "integer"
minimum = 1
maximum = 65535

[input_schema.properties.command]
type = "string"
minLength = 1

[input_schema.properties.command_args]
type = "array"
items = { type = "string" }

[input_schema.properties.cwd]
type = "string"

[input_schema.properties.env]
type = "object"
additionalProperties = { type = "string" }

[input_schema.properties.rows]
type = "integer"
minimum = 1
maximum = 65535
