schema_version = "1"
name = "plugin.companion_reconcile"
description = "Reconcile one local desktop companion plugin against desired state and boot policy."

[input_schema]
type = "object"
additionalProperties = false
required = ["package_id"]

[input_schema.properties.package_id]
type = "string"
minLength = 1

[input_schema.properties.package_version]
type = "string"
minLength = 1
