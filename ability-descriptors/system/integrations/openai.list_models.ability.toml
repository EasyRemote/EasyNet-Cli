schema_version = "3"
name = "openai.list_models"
descriptor_version = "1.0.0"
description = "OpenAI-compatible /v1/models served by the device daemon. Returns every host-local chat-base ability (`<agent>.chat`) the calling identity has dispatch grants on, projected as OpenAI `Model` objects whose `id` is the canonical agent-owned chat Ability URA."
exposure = "task"
dedicated_surface = "none"
subject_contract_kind = "authenticated-user"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"

[input_schema.properties.auth_token]
type = "string"
description = "Bearer token bound to a `<user>.api_key` entry on this host."
