schema_version = "2"
name = "federation.subscribe_directory"
descriptor_version = "1.0.0"
description = "Subscribe to legacy federation directory snapshots and deltas."
call_mode = "stream"
capability_state = "cutover_ready"
admission_action = "stream"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":true,\"streaming_only\":true,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = []
additionalProperties = false

[input_schema.properties.agent_ura]
type = "string"
description = "Optional Agent or Device URA filter."

[input_schema.properties.since_generation]
type = "integer"
description = "Optional generation cursor."
