schema_version = "3"
name = "pages.health"
descriptor_version = "1.0.0"
description = "Report daemon Pages registry readiness for this user or one project."
exposure = "operator"
dedicated_surface = "pages"
subject_contract_kind = "dedicated-surface"
call_mode = "rpc"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"

[input_schema.properties.project_id]
type = "string"
description = "Optional project id to check."

[input_schema.properties.surface_ref]
type = "string"
description = "Optional project resource URA to check."
