schema_version = "3"
name = "context.captures.read"
descriptor_version = "1.0.0"
description = "Stream one persisted media artifact as ordered, bounded chunks."
exposure = "task"
dedicated_surface = "none"
subject_contract_kind = "authenticated-user"
call_mode = "stream"
capability_state = "cutover_ready"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":true,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["id"]
additionalProperties = false

[input_schema.properties.id]
type = "string"
description = "Capture id from context.captures.list."
