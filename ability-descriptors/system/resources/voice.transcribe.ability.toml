schema_version = "2"
name = "voice.transcribe"
descriptor_version = "1.0.0"
description = "Stream audio in, receive transcription text out. True bidi: caller pushes audio BinaryChunk UP, callee returns text BinaryChunk (or structured JSON) DOWN. Subject MUST be an ASR-model resource_ura governed by the realm Authority."
call_mode = "bidi"
capability_state = "unsupported"
admission_action = "stream"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "null"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":true}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false

[input_schema.properties.format]
type = "string"
enum = ["opus"]

[input_schema.properties.language]
type = "string"
description = "ISO 639-1 code or `auto`."

[input_schema.properties.model_hint]
type = "string"
