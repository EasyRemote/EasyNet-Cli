schema_version = "2"
name = "voice.leave_call"
descriptor_version = "1.0.0"
description = "Mark a participant as having left an open call. Leaving does not end or reclassify the call; only `voice.end_call` transitions it to the terminal `VOICE_CALL_STATE_ENDED` state."
call_mode = "rpc"
capability_state = "seam"
admission_action = "invoke"
visibility = "SCOPED"
scope_subjects_kind = "any"
scope_subjects_uras = []
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{\"additionalProperties\":false,\"properties\":{\"call_id\":{\"type\":\"string\"},\"participant_id\":{\"type\":\"string\"}},\"required\":[\"call_id\",\"participant_id\"],\"type\":\"object\"}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["call_id", "participant_id"]
additionalProperties = false

[input_schema.properties.call_id]
type = "string"

[input_schema.properties.participant_id]
type = "string"

[input_schema.properties.reason]
type = "string"
