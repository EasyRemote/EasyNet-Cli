schema_version = "1"
name = "context.clipboard.list"
description = "List captured clipboard history as newest-first unique entries with duplicate counts and the tracking flag."

[input_schema]
type = "object"
additionalProperties = false

[input_schema.properties.limit]
type = "integer"
description = "Max unique entries to return (default 100, cap 200)."
