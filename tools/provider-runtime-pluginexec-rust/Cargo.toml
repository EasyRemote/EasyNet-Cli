[package]
name = "runtime-provider-pluginexec"
version = "0.1.0"
edition = "2021"
description = "Provider-scoped runtime declarative exec plugin sidecar helper"
license = "Apache-2.0"

[workspace]

[lib]
name = "runtime_provider_pluginexec"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
