schema_version = "2"
name = "browser.show_session"
descriptor_version = "1.0.0"
description = "Read the bounded lifecycle and protocol readiness projection for a browser session. The invocation subject MUST be the session resource URA and the caller MUST be the session creator. CDP endpoints, filesystem profile paths, cookies, and credentials are never returned."
call_mode = "rpc"
capability_state = "provider_backed"
admission_action = "read"
visibility = "SCOPED"
scope_subjects_kind = "only_ura_kinds"
scope_subjects_uras = ["resource"]
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":true,\"destructive\":false,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false
