schema_version = "3"
name = "browser.close_session"
descriptor_version = "1.0.0"
description = "Idempotently close a browser session. The target is closed, pending CDP calls are drained, an owned Chrome process is terminated, and session/profile capacity is released. A caller-supplied existing browser process is never terminated. The invocation subject MUST be the browser session resource URA."
exposure = "operator"
dedicated_surface = "browser"
subject_contract_kind = "dedicated-surface"
call_mode = "rpc"
capability_state = "provider_backed"
admission_action = "manage"
visibility = "SCOPED"
scope_subjects_kind = "only_ura_kinds"
scope_subjects_uras = ["resource"]
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":true,\"idempotent\":true,\"streaming_only\":false,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
additionalProperties = false
