schema_version = "2"
name = "remote_desktop.watch_events"
descriptor_version = "1.0.0"
description = "Watch remote desktop control events. v1 returns the bounded current snapshot through InvokeStream; live fan-out follows the canonical invocation contract."
call_mode = "stream"
capability_state = "provider_backed"
admission_action = "stream"
visibility = "SCOPED"
scope_subjects_kind = "only_ura_kinds"
scope_subjects_uras = ["resource"]
scope_agents_kind = "any"
scope_agents_uras = []
denied_agents = []
output_receipt_schema_json = "{}"
hints_json = "{\"read_only\":false,\"destructive\":false,\"idempotent\":false,\"streaming_only\":true,\"bidi_only\":false}"
receipt_semantics = "operational"

[input_schema]
type = "object"
required = ["session_id", "session_token"]
additionalProperties = false

[input_schema.properties.from_sequence]
type = "integer"
minimum = 0

[input_schema.properties.session_id]
type = "string"

[input_schema.properties.session_token]
type = "string"
