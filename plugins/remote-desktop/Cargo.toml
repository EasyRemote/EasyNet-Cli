[package]
name = "easynet-plugin-remote-desktop"
version = "0.148.60"
edition = "2021"
publish = false
description = "Package-owned native-static provider for EasyNet remote desktop abilities."

[lib]
name = "easynet_plugin_remote_desktop"
path = "src/lib.rs"

[dependencies]
easynet_cli = { package = "easynet", path = "../.." }
