# Execution checklist

- [x] Identify device directory projection as the root boundary.
- [x] Replace `tenant_id` row projection with `origin_realm`.
- [x] Update tests to assert no retired alias.
- [x] Extend SPEC v2 gate and self-test.
- [ ] Run targeted tests and convergence gates.
