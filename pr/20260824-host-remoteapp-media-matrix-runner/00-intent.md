# Intent

Close the executable seam between the real Browser RemoteApp lifecycle runner
and the existing three-scenario media adaptation verifier. A live run must
actually execute baseline, degraded-network, and receiver-backpressure sessions
against one selected Resource and aggregate only observed runtime/browser facts.
