# Invariants

1. Invocation history abilities remain read-only governance routes.
2. Tests must not derive authority from ambient host pairing or key service
   state.
3. Descriptor publication remains owned by the catalog's ledger governance
   owner.
4. The change must not add fallback identity, synthetic production credentials,
   or compatibility routing.
5. Public CLI, SDK, FFI, and daemon ability names remain unchanged.
