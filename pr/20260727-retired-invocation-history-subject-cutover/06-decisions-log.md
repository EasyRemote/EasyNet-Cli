2026-07-27:
- Treat `session/invocation_history:*` as the same retired carrier family as `session/invocation_history`.
- Do not preserve a rewrite path from retired session carriers to runtime-state read subjects.
- Keep all-zero placeholder rejection distinct from retired-carrier rejection where tuple validation sees the placeholder first.
