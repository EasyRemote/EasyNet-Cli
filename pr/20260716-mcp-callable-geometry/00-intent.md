# Intent

Close the MCP callable-geometry publication fork.

MCP `tools/list` is a unary `tools/call` catalog. Until the MCP provider owns a
stream/bidi implementation, only RPC descriptors may become MCP tools. Stream
and bidi descriptors remain visible through the daemon ability catalog, but
they must not be advertised as callable MCP tools.
