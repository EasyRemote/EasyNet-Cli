# Intent

Complete the Canonical Runtime Convergence V2 objective against the current
checkout evidence.

This slice keeps the architecture direction fixed: the SDK remains the
canonical runtime model, product-specific behavior stays in provider/daemon
boundaries, public invocation uses complete descriptor-bound tuples, and
downstream product evidence must prove real route/receipt behavior rather than
only API availability.
