Invariants
==========

- Runtime owner projections must use the same grammar at every layer.
- The authority binding grammar has exactly one realm Authority marker:
  `authority`.
- Product vocabulary is not valid runtime owner-state vocabulary.
- Retired `hub` projection strings fail closed rather than being repaired.
