# README factual convergence

Preserve the Runtime story, EAL example, roadmap, and operator flow. Add the
capability-native thesis, correct the installed process set and install command,
include EasyRemote, and defer changing command details to Clap help. The CI
check reads install artifacts from `dev-install-local.sh`; it does not prescribe
README prose.

Verification: `python3 tools/scripts/check_readme_facts.py --self-test && python3 tools/scripts/check_readme_facts.py`.
