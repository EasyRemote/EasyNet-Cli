# API contract

No public Ability or SDK contract changes. Unix behavior is unchanged.
Non-Unix `host_stream` now compiles and returns an explicit unsupported error
instead of making the entire runtime uncompilable.
