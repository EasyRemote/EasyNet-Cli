# API Contract

No public API changes.

The only updated artifacts are conformance adapter evidence reports under
`sdk/conformance/runner`. These reports are consumed by cutover/readiness gates
and do not alter runtime behavior.
