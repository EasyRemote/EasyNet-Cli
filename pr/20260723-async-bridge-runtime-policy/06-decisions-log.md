# Decisions Log

## 2026-07-23

- Treat the bridge as a small explicit state machine instead of a fallback
  helper. The behavior is intentionally preserved while the ownership language
  converges.
- Update historical design/RFC references because stale architecture guidance is
  an implementation hazard even when it does not affect compilation.
