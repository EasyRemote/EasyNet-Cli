Decision log:

- Preserve ABI integer compatibility and use typed last-error projection for the
  canonical runtime model.
- Do not add carrier-specific patches; fix the shared daemon status boundary.
