2026-07-27:
- Treat the Pages listener path as a bounded HTTP projection adapter until a real canonical Invocation cutover exists.
- Remove the pseudo-ability module name instead of preserving a compatibility alias.
- Gate false canonical-dispatch vocabulary because it masks missing receipt-chain coverage.
