2026-07-27:
- Treat `pages_port` as provider wire extension metadata, not SDK runtime lifecycle state.
- Preserve attach compatibility with current daemon discovery files by accepting the field and discarding it.
- Do not add a replacement Pages SDK field; Pages belongs to product APIs downstream of the canonical runtime SDK.
