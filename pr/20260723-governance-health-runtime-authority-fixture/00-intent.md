# Intent

Remove ambient runtime catalog authority construction from the governance health
dispatch smoke test.

`observe.health` is the minimal local dispatch probe. Its executable test must
continue to prove the runtime-backed dispatch path while binding the catalog to
an explicit Device authority root.
