# Invariants

1. `observe.health` production registration behavior remains unchanged.
2. The dispatch smoke test remains runtime-backed and executable.
3. The test catalog uses an explicit Device authority root.
4. No fallback identity, synthetic signer, or compatibility admission path is
   introduced.
5. The public `observe.health` response contract remains unchanged.
