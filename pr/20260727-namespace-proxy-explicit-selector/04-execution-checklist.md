1. Add explicit nullable selector type in federation wrappers.
2. Change `NamespaceProxyResolveRequest.ability_name` to required nullable selector.
3. Update dispatcher validator and peer argument serialization.
4. Update namespace proxy tests for explicit null selector and missing-field rejection.
5. Update descriptor schema required fields.
6. Update SPEC v2 gate and self-test fixture.
7. Run focused Rust tests and convergence gates.
