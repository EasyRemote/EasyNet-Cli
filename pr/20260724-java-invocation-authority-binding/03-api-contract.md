# API Contract

No public API is changed.

Observable behavior change:

- Java SDK now rejects tuple-bound authority mismatches before transport with canonical authority error codes.
- Valid tuple-bound authority metadata remains accepted.

