# API Contract

- Accepted fields: `easynet_rendezvous`, `static_hubs`.
- Unknown fields: invalid configuration.
- Valid resolution outputs are unchanged for current config.
- No URI terminology or product-specific fields are introduced.
