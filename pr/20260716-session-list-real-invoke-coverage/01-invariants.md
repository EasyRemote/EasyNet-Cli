# Invariants

1. `session.list` is invoked through the live local dispatcher.
2. The test route must equal the generated canonical route constant.
3. The coverage gate sees an explicit public ability declaration in the test
   source.
4. No production capability, route, or public response changes.
