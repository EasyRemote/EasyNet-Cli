# Invariants

- A headless Linux environment must not be represented as a successful companion load.
- Unsupported session remains non-fatal to daemon boot.
- Runtime session detection must be explicit and testable.
- Platform adapters must not duplicate session detection logic already owned by the companion runtime model.
