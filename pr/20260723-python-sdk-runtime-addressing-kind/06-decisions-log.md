# Decisions Log

## 2026-07-23

- Keep public projection values stable. The issue is internal ownership
  vocabulary, not the JSON API contract.
- Keep `hub` as the public projection kind for authority URAs. This iteration
  only removes product-shaped private helper ownership from the Python SDK root.
