# API Contract

- No exported Python SDK symbols are renamed.
- No JSON projection fields are renamed.
- No package import paths are changed.
- Tests continue to assert the same public `kind` and `owner_kind` values.
