# Invariants

- Public Addressing projection JSON remains unchanged.
- `authority` still projects as public runtime kind `hub`.
- Ability owner kind projection remains stable.
- No product/provider import is introduced into `easynet_sdk.axon_addressing`.
- Provider-specific EasyNet behavior remains under `providers/easynet`.
