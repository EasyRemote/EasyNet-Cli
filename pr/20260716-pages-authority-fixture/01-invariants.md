# Invariants

1. A hosted Agent root must share the authority context Device's realm.
2. Process-wide test registries must not derive identity from mutable local
   credential state.
3. Pages manifest dispatch remains registered under the canonical Agent owner.
