Decisions:
- Reuse existing Signer and SignerHandle abstractions instead of adding a product-specific signer type.
- Preserve existing public signing APIs.
- Keep live daemon keyring policy out of this SDK slice.
