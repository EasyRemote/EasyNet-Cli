# Execution Checklist

- [x] Add Go Admin Runtime ability constants and args for `session.create` and
  `session.delete`.
- [x] Implement `CreateDeviceSession` and `DeleteDeviceSession` via Runtime
  Core invocation.
- [x] Preserve device/session facts in DTO projections.
- [x] Add focused Go runtime tests for invocation args, descriptor delegation,
  and projected results.
- [x] Run Go SDK checks and update parity/plan evidence.
