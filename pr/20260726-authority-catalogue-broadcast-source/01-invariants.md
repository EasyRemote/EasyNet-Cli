Invariants
==========

- Catalogue `source` metadata is runtime presentation metadata, not a product
  deployment label.
- Realm-scope catalogue rows are published by the realm Authority.
- Product Hub vocabulary must not appear in canonical catalogue DTO metadata.
- Federation wire-field migration is intentionally separate from presentation
  metadata migration.
