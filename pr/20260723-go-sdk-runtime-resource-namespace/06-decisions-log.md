# Decisions Log

## 2026-07-23

- Keep this as an internal SDK-root naming convergence slice. Moving
  `provider/easynet` is intentionally out of scope because it changes import
  topology and needs a broader provider package plan.
