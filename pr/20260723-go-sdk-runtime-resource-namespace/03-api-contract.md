# API Contract

- No exported Go symbols are renamed.
- No package import paths are changed.
- No generated conformance manifest update is required unless source hashes are
  intentionally refreshed by a broader SDK inventory pass.
- Root SDK comments must not describe resource namespace projection as an
  EasyNet product adapter.
