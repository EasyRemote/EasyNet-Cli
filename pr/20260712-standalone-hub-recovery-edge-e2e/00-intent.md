Goal: strengthen standalone-Hub recovery UX evidence on the real TCP+TLS Hub
daemon path.

This slice extends the existing Hub URA TLS join E2E with recovery replay and
deleted-principal recovery terminality assertions. It does not introduce a
second login system, Backend account model or product-specific SDK recovery
abstraction.
