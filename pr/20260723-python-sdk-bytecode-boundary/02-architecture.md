# Architecture

The SDK repository owns canonical runtime source and provider seams.
Interpreter bytecode is downstream execution state and has no semantic role in
the canonical runtime model.

The enforcement point is the SPEC v2 gate because the defect affects SDK source
ownership and product/provider boundary evidence.
