# Risk

The resolver is stricter for User URAs and still supports runtime owners. Since the boot and trust auto-wire paths derive Device/Hub URAs, behavior should remain equivalent while removing duplicate loader policy.

