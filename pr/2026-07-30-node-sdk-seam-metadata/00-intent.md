# Intent

Restore Node SDK package metadata to the declared seam capability state.

The Node SDK package is not cutover-ready release evidence yet; its package version must remain `0.0.0-seam` until the capability matrix moves it forward.

