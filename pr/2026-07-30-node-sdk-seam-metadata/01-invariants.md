# Invariants

1. Node SDK package metadata must reflect seam state, not CLI product release version.
2. The package remains private while in seam state.
3. Package name remains generic runtime SDK naming.
4. Metadata checks must fail if this package claims unsupported release stability.

