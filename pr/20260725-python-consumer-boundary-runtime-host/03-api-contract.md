# API Contract

- `audit_consumer_boundary` remains the public entry point.
- `BoundaryViolation` shape is unchanged.
- The retired rule string `raw_daemon_session` is replaced by
  `raw_runtime_host_session`.
