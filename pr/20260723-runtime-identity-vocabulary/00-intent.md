# Intent

Remove product-shaped vocabulary from daemon-local runtime identity comments and
pin the boundary in the canonical runtime convergence gate.

This is a convergence slice, not a feature slice. The public runtime API,
ability names, route behavior, signer custody model, and receipt semantics must
remain unchanged.
