# Intent

Determine whether the existing ABI v8 raw stream work is actually complete for
RemoteApp/EasyRemote high-frequency media use, then close concrete gaps without
changing Invocation semantics or weakening ABI v7 compatibility.

The audit must distinguish an exported symbol from a usable product data path.
