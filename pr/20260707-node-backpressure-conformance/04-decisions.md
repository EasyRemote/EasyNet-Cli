# Decisions

1. Use the existing Node Runtime Core state-machine tests as direct evidence.
2. Do not add daemon provider code in this slice.
3. Update Node documentation to describe bounded stream/bidi backpressure as a
   seam-level Runtime Core invariant rather than a missing C ABI provider claim.
