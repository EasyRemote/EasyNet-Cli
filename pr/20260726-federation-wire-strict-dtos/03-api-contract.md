# API Contract

- Accepted fields remain the current Rust DTO fields.
- Unknown fields are invalid input/output for daemon federation contracts.
- Field names use URA terminology only.
- SDK surfaces remain unchanged.
