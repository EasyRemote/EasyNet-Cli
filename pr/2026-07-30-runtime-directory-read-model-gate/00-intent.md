# Intent

Close the live runtime directory readiness gap behind C440/T11 read-model failures.

The session contract proves transport admission only. Public product readiness must not become `ConnectedOnline` until the daemon has published its dynamic owner projection and the Hub read model can resolve the device through canonical directory records.

