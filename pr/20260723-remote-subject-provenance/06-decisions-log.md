# Decisions Log

- Treat subject selection as provenance state rather than a general derivation
  helper.
- Keep daemon-selected target-owned subjects only for named daemon/root issuers;
  do not expose them as a public omission policy.
