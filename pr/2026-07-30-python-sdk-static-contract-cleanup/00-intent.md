# Intent

Close the Python SDK static-contract failure surfaced by the broader cutover-readiness gate.

The fix is limited to naming hygiene in canonical stream/bidi field rejection helpers; it does not change SDK wire behavior.

