# Invariants

1. Unknown stream/bidi fields remain rejected fail-closed.
2. Error messages still name the noncanonical field.
3. No runtime DTO fields or lifecycle semantics change.
4. Python SDK static contract must pass without suppressing lint rules.

