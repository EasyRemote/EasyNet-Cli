schema_version = "1"
name = "node.deregister"
description = "Inverse of node.register at shutdown. v1 acknowledges intent; federation fan-out lands with the Invoke replacement."

[input_schema]
type = "object"
additionalProperties = false
