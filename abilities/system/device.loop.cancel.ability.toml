schema_version = "1"
name = "device.loop.cancel"
description = "Cancel an in-flight loop. Already-terminal loops (Done / Exhausted / VerifyMalformed) are untouched."

[input_schema]
type = "object"
required = ["loop_id"]
additionalProperties = false

[input_schema.properties.loop_id]
type = "string"
