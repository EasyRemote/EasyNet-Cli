schema_version = "1"
name = "device.skill.list"
description = "List skills installed for an agent (or all agents). Thin facade over fleet.list_abilities; returns the same InstalledSkill row shape."

[input_schema]
type = "object"

[input_schema.properties.owner_agent_id]
type = "string"
description = "Optional. Filter to skills owned by this agent. Absent = all agents."
