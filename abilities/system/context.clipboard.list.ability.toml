schema_version = "1"
name = "context.clipboard.list"
description = "List captured clipboard history (newest first) with the tracking flag."

[input_schema]
type = "object"
additionalProperties = false

[input_schema.properties.limit]
type = "integer"
description = "Max entries to return (default 100, cap 200)."
