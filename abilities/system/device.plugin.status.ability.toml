schema_version = "1"
name = "device.plugin.status"
description = "Report plugin package load planning and actual daemon runtime registration status."

[input_schema]
type = "object"
additionalProperties = false
