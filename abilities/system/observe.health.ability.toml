schema_version = "1"
name = "observe.health"
description = "Round-trip smoke ability. Returns the caller's args plus a daemon timestamp. Use to confirm the dispatch path is wired between peer and local handlers."

[input_schema]
type = "object"
additionalProperties = true
